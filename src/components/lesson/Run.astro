---
/**
 * <Run> — その場で実行できるコード例（20-platform.md 第2.3節）。
 * 本文には実行結果を併記し（10-lesson 第2.2節）、▶ で実際に動かせる。
 */
import RunBox from '../../lesson/ui/RunBox';
import { nextId } from '../../lesson/ui/ids';

interface Props {
  /** 実行できる完全なコード。断片は置かない */
  code: string;
  /** 本文に併記する実行結果 */
  out?: string;
  /** input() を使うときの入力欄の初期値 */
  stdin?: string;
}

const { code, out = '', stdin } = Astro.props;
const key = nextId('run');
---

<div class="kit-runwrap">
  <RunBox client:visible code={code} expected={out} stdin={stdin} editorId={key} />
</div>
