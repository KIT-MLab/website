---
id: weekly-2026-10-20
date: '2026-10-20'
title: 第7〜8章と練習編2
chapters: ['00-start', '01-python', '02-numbers', '03-branch', '04-loop', '04p-practice1', '05-function', '06-error', '07-array', '08-table', '08p-practice2']
---

{/* はじめに */}

第7章から第8章と、練習編2までの範囲で6問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-10-20-b1"
  kind="build"
  forbid={['for ']}
  stdin={`10
12
9
15
11`}
  tests={[
    { kind: 'stdout', stdin: `10\n12\n9\n15\n11\n` },
    { kind: 'stdout', stdin: `10\n10\n10\n10\n10\n` },
    { kind: 'stdout', stdin: `0\n5\n10\n15\n20\n` },
    { kind: 'stdout', stdin: `-5\n0\n5\n10\n15\n` },
  ]}
  hints={[
    '5つの値を配列にしてから `mean()` で平均を求めます。第7章1節（numpyの配列と要素ごとの計算）と第7章3節（平均・合計・最大と軸）を読んでください。',
    '平均より小さい値だけを取り出すには、比べた結果をそのまま添字にします。第7章4節（条件やスライスで取り出す）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'input', 'compare']}
>
5人の50m走のタイム（秒）を配列にし、平均より速かった（値が平均より小さい）人のタイムだけを取り出してください。for文は使わないでください。

<Input>
- 1行目から5行目: 5人のタイム（1行に1つ、整数）
</Input>

<Output>
取り出した配列を、入力の順のまま `print()` にそのまま渡して、1行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-20-b5"
  kind="build"
  stdin={`3
15`}
  tests={[
    { kind: 'stdout', stdin: `3\n15\n` },
    { kind: 'stdout', stdin: `1\n20\n` },
    { kind: 'stdout', stdin: `0\n10\n` },
    { kind: 'stdout', stdin: `5\n0\n` },
  ]}
  hints={[
    '1段目からn段目までの高さは、1からnまでの数の並びに1段の高さをかけると求まります。',
    '決まった並びの配列を一度に作るには `np.arange()` を使います。第8章4節（並びを一度に作る）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'operators', 'input', 'print']}
>
階段の段の数と1段の高さから、1段目からその段までの高さ（床からの高さ）を求めてください。

<Input>
- 1行目: 段の数（整数）
- 2行目: 1段の高さ（cm、整数）
</Input>

<Output>
1段目からその段までの高さを、配列のまま `print()` で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-20-b6"
  kind="build"
  forbid={['for ']}
  stdin={`80
70
90
60
50
40`}
  tests={[
    { kind: 'stdout', stdin: `80\n70\n90\n60\n50\n40\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `-5\n10\n-5\n10\n-5\n10\n` },
  ]}
  hints={[
    'reshapeで3行2列の表にしてから、行と列を入れ替えます。',
    '行と列を入れ替えるのは `.T` です。第8章2節（転置）と第8章5節（reshapeで表にする）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'input', 'print']}
>
3人ぶんの国語と数学の点数を読み取り、reshapeで3行2列の表にしてから、転置して教科ごとの行に並べ替えてください。for文は使わないでください。

<Input>
- 1行目から6行目: 点数（1行に1つ、整数）。1人ずつ、国語→数学の順に2つ
</Input>

<Output>
転置した表を `print()` にそのまま渡して表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-20-b2"
  kind="build"
  forbid={['for ']}
  stdin={`80
70
90
60
50
40`}
  tests={[
    { kind: 'stdout', stdin: `80\n70\n90\n60\n50\n40\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `100\n100\n50\n50\n0\n100\n` },
    { kind: 'stdout', stdin: `-10\n20\n-5\n15\n0\n10\n` },
  ]}
  hints={[
    'reshapeで表にしてから、`table[:, 1]` のように列を指定して取り出します。第8章1節（行と列を指定する添字）と第8章5節（reshapeで表にする）を読んでください。',
    '取り出した列にそのまま `mean()` が使えます。第7章3節（平均・合計・最大と軸）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'round-math', 'input']}
>
3人ぶんの2回のテストの点数を、reshapeで3行2列の表にしてください。2回目のテスト（添字1の列）の平均を、小数第1位まで丸めて求めます。for文は使わないでください。

<Input>
- 1行目から6行目: 点数（1行に1つ、整数）。1人ずつ、1回目→2回目の順に2つ
</Input>

<Output>
2回目のテストの平均を1行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-20-b3"
  kind="build"
  stdin={`3
80
50
90
40
70
60
65
65
65`}
  tests={[
    { kind: 'stdout', stdin: `3\n80\n50\n90\n40\n70\n60\n65\n65\n65\n` },
    { kind: 'stdout', stdin: `1\n60\n60\n60\n` },
    { kind: 'stdout', stdin: `1\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `2\n100\n100\n100\n0\n0\n0\n` },
  ]}
  hints={[
    '条件を満たすかどうかの配列は、真偽をそのまま数として `sum(axis=0)` に渡せます。第7章4節（条件やスライスで取り出す）と第7章3節（平均・合計・最大と軸）を読んでください。',
    '既定値のある引数の書き方は、第5章3節（引数と既定値）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'function', 'compare', 'input']}
>
合格者の人数を科目ごとに数える関数 `pass_count(table, border=60)` を書いてください。`table` は3教科の点数の表で、点数が `border` 以上なら合格です。

読み取った値は、reshapeで人数行3列の表にしてから渡します。

<Input>
- 1行目: 人数（整数）
- 2行目から: 人数ぶんの3教科の点数（1行に1つ、整数）。1人ずつ3つ
</Input>

<Output>
既定の基準での人数と、基準を `70` にしたときの人数を、この順に2行で表示します。どちらも、関数が返した配列を `print()` にそのまま渡します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-20-b7"
  kind="build"
  stdin={`3
80
70
90
85
70
95`}
  tests={[
    { kind: 'stdout', stdin: `3\n80\n70\n90\n85\n70\n95\n` },
    { kind: 'stdout', stdin: `1\n50\n50\n` },
    { kind: 'stdout', stdin: `2\n80\n80\n70\n90\n` },
    { kind: 'stdout', stdin: `3\n0\n0\n0\n0\n0\n1\n` },
  ]}
  hints={[
    '1回目と2回目の点数をそれぞれ配列にし、2回目から1回目を引くと、科目ごとの伸びが求まります。',
    '伸びが0より大きいかどうかの配列を、そのまま添字にすると、上がった科目だけを取り出せます。第7章4節（条件やスライスで取り出す）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'compare', 'input', 'print']}
>
n教科の1回目と2回目のテストの点数から、2回目に点数が上がった教科の、2回目の点数だけを取り出してください。

<Input>
- 1行目: 教科の数（整数）
- 2行目から: 1回目の点数（1行に1つ、整数、教科の数ぶん）
- 続けて: 2回目の点数（1行に1つ、整数、教科の数ぶん）
</Input>

<Output>
取り出した配列を `print()` にそのまま渡して、1行で表示します。
</Output>
</Exercise>
