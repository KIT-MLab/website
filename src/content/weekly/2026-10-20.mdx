---
id: weekly-2026-10-20
date: '2026-10-20'
title: 第7〜8章と練習編2
chapters: ['00-start', '01-python', '02-numbers', '03-branch', '04-loop', '04p-practice1', '05-function', '06-error', '07-array', '08-table', '08p-practice2']
---

{/* はじめに */}

第7章から第8章と、練習編2までの範囲で6問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-10-20-c1"
  kind="choose"
  tests={[{ kind: 'choice', correct: 1 }]}
  hints={[
    'axis=1は科目をまたぐ方向、つまり1人分の平均です。第7章3節（平均・合計・最大と軸）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'errors']}
>
次のコードを実行すると、何が表示されますか。

```python
import numpy as np

scores = np.array([[80, 60], [40, 100]])
print(scores.mean(axis=1))
```

- `[70. 70.]`
- `[60. 80.]`
- `70.0`
- `AxisError` になる
</Exercise>

<Exercise
  id="weekly-2026-10-20-c2"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[
    'reshapeの行数×列数が、元の個数と合っているか確かめてください。第8章5節（reshapeで表にする）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'errors']}
>
次のコードを実行すると、どうなりますか。

```python
import numpy as np

flat = np.array([1, 2, 3, 4, 5, 6])
print(flat.reshape(2, 4))
```

- 2行4列の表になる
- `ValueError` になる
- `[1 2 3 4]` と表示される
- `[[1 2 3 4] [5 6]]` と表示される
</Exercise>

<Exercise
  id="weekly-2026-10-20-b1"
  kind="build"
  forbid={['for ']}
  stdin={`10
12
9
15
11`}
  tests={[
    { kind: 'stdout', stdin: `10\n12\n9\n15\n11\n` },
    { kind: 'stdout', stdin: `10\n10\n10\n10\n10\n` },
    { kind: 'stdout', stdin: `0\n5\n10\n15\n20\n` },
    { kind: 'stdout', stdin: `-5\n0\n5\n10\n15\n` },
  ]}
  hints={[
    '5つの値を配列にしてから `mean()` で平均を求めます。第7章1節（numpyの配列と要素ごとの計算）と第7章3節（平均・合計・最大と軸）を読んでください。',
    '平均より小さい値だけを取り出すには、比べた結果をそのまま添字にします。第7章4節（条件やスライスで取り出す）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'input', 'compare']}
>
入力欄から5人の50m走のタイム（秒）を読み取り、配列にしてください。平均のタイムを求め、平均より速かった（値が平均より小さい）人のタイムだけを、この順のまま表示してください。

取り出した配列は、そのまま `print()` に渡します。for文は使わないでください。
</Exercise>

<Exercise
  id="weekly-2026-10-20-b2"
  kind="build"
  forbid={['for ']}
  stdin={`80
70
90
60
50
40`}
  tests={[
    { kind: 'stdout', stdin: `80\n70\n90\n60\n50\n40\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `100\n100\n50\n50\n0\n100\n` },
    { kind: 'stdout', stdin: `-10\n20\n-5\n15\n0\n10\n` },
  ]}
  hints={[
    'reshapeで表にしてから、`table[:, 1]` のように列を指定して取り出します。第8章1節（行と列を指定する添字）と第8章5節（reshapeで表にする）を読んでください。',
    '取り出した列にそのまま `mean()` が使えます。第7章3節（平均・合計・最大と軸）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'round-math', 'input']}
>
入力欄から3人ぶん、2回のテストの点数を読み取ってください（1行に1つの数、1人ずつ1回目→2回目の順で2つ）。reshapeで3行2列の表にしてください。

2回目のテスト（添字1の列）の平均を、小数第1位まで丸めて表示してください。for文は使わないでください。
</Exercise>

<Exercise
  id="weekly-2026-10-20-b3"
  kind="build"
  stdin={`3
80
50
90
40
70
60
65
65
65`}
  tests={[
    { kind: 'stdout', stdin: `3\n80\n50\n90\n40\n70\n60\n65\n65\n65\n` },
    { kind: 'stdout', stdin: `1\n60\n60\n60\n` },
    { kind: 'stdout', stdin: `1\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `2\n100\n100\n100\n0\n0\n0\n` },
  ]}
  hints={[
    '条件を満たすかどうかの配列は、真偽をそのまま数として `sum(axis=0)` に渡せます。第7章4節（条件やスライスで取り出す）と第7章3節（平均・合計・最大と軸）を読んでください。',
    '既定値のある引数の書き方は、第5章3節（引数と既定値）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'function', 'compare', 'input']}
>
入力欄の1行目を人数として読み取ってください。続けて、人数ぶん3教科の点数を読み取ってください（1行に1つの数、1人ずつ3つ）。読み取った値はreshapeで人数行3列の表にしてください。

合格者の人数を科目ごとに数える関数 `pass_count(table, border=60)` を書いてください。既定の基準での人数と、基準を `70` にしたときの人数を、`[2 2 3]` のような配列の形で、この順に表示してください。
</Exercise>

<Exercise
  id="weekly-2026-10-20-b4"
  kind="build"
  stdin={`3
30
35
40
38
20
20`}
  tests={[
    { kind: 'stdout', stdin: `3\n30\n35\n40\n38\n20\n20\n` },
    { kind: 'stdout', stdin: `1\n10\n10\n` },
    { kind: 'stdout', stdin: `1\n0\n5\n` },
    { kind: 'stdout', stdin: `2\n50\n40\n20\n30\n` },
  ]}
  hints={[
    'reshapeで2列の表にしてから、`table[:, 1] - table[:, 0]` のように列どうしを引き算します。第8章1節（行と列を指定する添字）と第7章1節（numpyの配列と要素ごとの計算）を読んでください。',
    '0より大きい値の個数は、条件を満たす配列の `sum()` で数えられます。第7章4節（条件やスライスで取り出す）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['numpy', 'function', 'compare', 'input']}
>
入力欄の1行目を人数として読み取ってください。続けて、人数ぶん1回目と2回目の反復横跳びの記録を読み取ってください（1行に1つの数、1人ずつ1回目→2回目の順で2つ）。読み取った値はreshapeで人数行2列の表にしてください。

2回目から1回目を引いた伸びを求める関数 `improvement(table)` を書いてください。伸びの配列をそのまま表示してください。続けて、伸びが `0` より大きかった人数を表示してください。
</Exercise>
