---
id: weekly-2026-09-29
date: '2026-09-29'
title: 第0〜1章
chapters: ['00-start', '01-python']
---

{/* はじめに */}

第0章と第1章の範囲から4問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-09-29-c1"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[
    '第0章2節（半角と全角）と第1章1節（printで値を表示する）を読んでください。',
  ]}
  mistakes={[]}
>
次の4つのうち、そのまま実行して `こんにちは` と表示されるものはどれですか。記号が半角か全角かに気をつけて見てください。

- `print（"こんにちは"）`
- `print("こんにちは")`
- `print(”こんにちは”)`
- `Print("こんにちは")`
</Exercise>

<Exercise
  id="weekly-2026-09-29-b1"
  kind="build"
  stdin={`750
1000`}
  tests={[
    { kind: 'stdout', stdin: `750\n1000\n` },
    { kind: 'stdout', stdin: `1000\n1000\n` },
    { kind: 'stdout', stdin: `1\n10000\n` },
  ]}
  hints={[
    '入力欄から受け取った値は文字列です。int() で数に直してから引き算します。第1章2節（inputで値を受け取る）を読んでください。',
    '値段と払ったお金を、それぞれ変数に入れておくと式が読みやすくなります。第1章3節（変数に値を残す）と第1章4節（f文字列で文章にまとめる）を読んでください。',
  ]}
  mistakes={[]}
>
お店のレジのプログラムを作ります。入力欄の1行目で値段を、2行目で払ったお金を、どちらも整数で受け取ってください。

値段が750円で1000円払ったときは、`おつりは 250 円です` と表示してください。値と円の間には、半角の空白を1つずつ入れます。
</Exercise>

<Exercise
  id="weekly-2026-09-29-b2"
  kind="build"
  stdin={`80
70
90`}
  tests={[
    { kind: 'stdout', stdin: `80\n70\n90\n` },
    { kind: 'stdout', stdin: `100\n100\n100\n` },
    { kind: 'stdout', stdin: `0\n55\n0\n` },
  ]}
  hints={[
    '3教科の合計を求めて変数に入れる形は、第1章5節（点数表を作る）と同じです。',
    '満点は3教科で300点です。あと何点かは、300から合計を引くと求められます。',
  ]}
  mistakes={[]}
>
入力欄から、国語・数学・英語の点数を、この順に1行ずつ整数で受け取ってください。

1行目に3教科の点数、2行目に合計、3行目に満点の300点まであと何点かを表示してください。国語80点・数学70点・英語90点のときは、次のように表示します。

```text
国語 80点 数学 70点 英語 90点
合計 240点
満点まであと 60点
```
</Exercise>

<Exercise
  id="weekly-2026-09-29-b3"
  kind="build"
  stdin={`report`}
  tests={[
    { kind: 'stdout', stdin: `report\n` },
    { kind: 'stdout', stdin: `第1回\n` },
    { kind: 'stdout', stdin: `a\n` },
    { kind: 'stdout', stdin: `0\n` },
  ]}
  hints={[
    'Python のプログラムを保存するファイルの名前は、末尾が .py です。第0章6節（拡張子）を読んでください。',
    '文字列どうしは + でつなげられます。第1章4節（f文字列で文章にまとめる）を読んでください。',
  ]}
  mistakes={[]}
>
入力欄から、ファイルの名前を拡張子なしで受け取ってください。

受け取った名前の末尾に `.py` を付け、`保存するファイル名は report.py です` のように表示してください。名前が `report` のときの例です。
</Exercise>
