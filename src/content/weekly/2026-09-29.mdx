---
id: weekly-2026-09-29
date: '2026-09-29'
title: 第0〜2章
chapters: ['00-start', '01-python', '02-numbers']
---

{/* はじめに */}

第0章から第2章までの範囲で6問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-09-29-c1"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[
    '第0章2節（半角と全角）と第1章1節（printで値を表示する）を読んでください。',
  ]}
  mistakes={[]}
>
次の4つのうち、そのまま実行して `こんにちは` と表示されるものはどれですか。記号が半角か全角かに気をつけて見てください。

- `print（"こんにちは"）`
- `print("こんにちは")`
- `print(”こんにちは”)`
- `Print("こんにちは")`
</Exercise>

<Exercise
  id="weekly-2026-09-29-c2"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[
    '第2章3節（べき乗と余りを求める）と第2章4節（計算の順番とかっこ）を読んでください。',
  ]}
  mistakes={[]}
>
次のコードを実行すると、表示される数はどれですか。

```python
print(10 - 4 // 2)
```

- `3`
- `8`
- `9`
- `4`
</Exercise>

<Exercise
  id="weekly-2026-09-29-b1"
  kind="build"
  stdin={`750
1000`}
  tests={[
    { kind: 'stdout', stdin: `750\n1000\n` },
    { kind: 'stdout', stdin: `1000\n1000\n` },
    { kind: 'stdout', stdin: `1\n10000\n` },
  ]}
  hints={[
    '入力欄から受け取った値は文字列です。int() で数に直してから引き算します。第1章2節（inputで値を受け取る）を読んでください。',
    '値段と払ったお金を、それぞれ変数に入れておくと式が読みやすくなります。第1章3節（変数に値を残す）と第1章4節（f文字列で文章にまとめる）を読んでください。',
  ]}
  mistakes={[]}
>
お店のレジのプログラムを作ります。入力欄の1行目で値段を、2行目で払ったお金を、どちらも整数で受け取ってください。

値段が750円で1000円払ったときは、`おつりは 250 円です` と表示してください。値と円の間には、半角の空白を1つずつ入れます。
</Exercise>

<Exercise
  id="weekly-2026-09-29-b2"
  kind="build"
  stdin={`3
4`}
  tests={[
    { kind: 'stdout', stdin: `3\n4\n` },
    { kind: 'stdout', stdin: `0\n0\n` },
    { kind: 'stdout', stdin: `5\n5\n` },
    { kind: 'stdout', stdin: `6\n8\n` },
  ]}
  hints={[
    '第2章5節（mathモジュールを使う）を読み、平方根を求める関数を確認してください。対角線の長さは、横の2乗と縦の2乗の和の平方根です。',
    '小数第1位に丸める書き方は、第2章2節（round()で桁を整える）を読んでください。',
  ]}
  mistakes={[]}
>
長方形の横の長さと縦の長さを、この順に入力欄から読み取ってください。対角線の長さを `math.sqrt` を使って求めてください。小数第1位まで丸めて `対角線 5.0` の形で表示してください。
</Exercise>

<Exercise
  id="weekly-2026-09-29-b3"
  kind="build"
  stdin={`80
70
90`}
  tests={[
    { kind: 'stdout', stdin: `80\n70\n90\n` },
    { kind: 'stdout', stdin: `100\n100\n100\n` },
    { kind: 'stdout', stdin: `0\n55\n0\n` },
  ]}
  hints={[
    '3教科の合計を求めて変数に入れる形は、第1章5節（点数表を作る）と同じです。',
    '平均は、合計を3で割って求めます。第2章2節（round()で桁を整える）で丸め方を確認してください。',
  ]}
  mistakes={[]}
>
入力欄から、国語・数学・英語の点数を、この順に1行ずつ整数で受け取ってください。

1行目に3教科の点数、2行目に合計、3行目に満点の300点まであと何点かを表示してください。続けて4行目に、平均を小数第1位まで丸めて表示してください。国語80点・数学70点・英語90点のときは、次のように表示します。

```text
国語 80点 数学 70点 英語 90点
合計 240点
満点まであと 60点
平均 80.0点
```
</Exercise>

<Exercise
  id="weekly-2026-09-29-b4"
  kind="build"
  stdin={`10000
3`}
  tests={[
    { kind: 'stdout', stdin: `10000\n3\n` },
    { kind: 'stdout', stdin: `9000\n3\n` },
    { kind: 'stdout', stdin: `0\n4\n` },
    { kind: 'stdout', stdin: `100\n1\n` },
  ]}
  hints={[
    '1人あたりの金額は整数除算 `//`、余った金額は剰余 `%` で求めます。第2章3節（べき乗と余りを求める）を読んでください。',
  ]}
  mistakes={[]}
>
入力欄の1行目を合計金額として、2行目を人数として、それぞれ整数で読み取ってください。1人あたりの金額は、端数を切り捨てて求めます。求めた金額と、割り切れずに余った金額を `1人 3333円 余り 1円` の形で表示してください。
</Exercise>
