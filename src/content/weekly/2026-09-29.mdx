---
id: weekly-2026-09-29
date: '2026-09-29'
title: 第1〜2章
chapters: ['01-python', '02-numbers']
---

{/* はじめに */}

第1章と第2章から6問です。前半は復習寄りなので、サッと解いてください。後半が今日の本番で、困ったら問題に書いてある節やヒントを使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-09-29-b1"
  kind="build"
  stdin={`125`}
  tests={[
    { kind: 'stdout', stdin: `125\n` },
    { kind: 'stdout', stdin: `0\n` },
    { kind: 'stdout', stdin: `60\n` },
    { kind: 'stdout', stdin: `3599\n` },
  ]}
  hints={[
    '割り算の商は `//`、余りは `%` で求めます。第2章3節（べき乗と余りを求める）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
秒数が何分何秒になるかを求めてください。

<Input>
- 1行目: 秒数（整数）
</Input>

<Output>
読み取った秒数と、それが何分何秒かを1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-09-29-b2"
  kind="build"
  stdin={`5
24`}
  tests={[
    { kind: 'stdout', stdin: `5\n24\n` },
    { kind: 'stdout', stdin: `0\n30\n` },
    { kind: 'stdout', stdin: `42.195\n180\n` },
  ]}
  hints={[
    '距離は小数なので、第2章1節（intとfloatを使い分ける）を読んでください。',
    '時速は、距離を「かかった時間（時間）」で割った値です。分を時間に直すには60で割ります。丸め方は第2章2節（round()で桁を整える）にあります。',
  ]}
  mistakes={[]}
  syntax={['input', 'round-math', 'operators', 'print']}
>
走った距離とかかった時間から、時速を求めてください。時速は小数第1位まで丸めます。

<Input>
- 1行目: 走った距離（km、小数）
- 2行目: かかった時間（分、整数）
</Input>

<Output>
時速を1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-09-29-b3"
  kind="build"
  stdin={`150
3
400
2`}
  tests={[
    { kind: 'stdout', stdin: `150\n3\n400\n2\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `99\n1\n0\n0\n` },
    { kind: 'stdout', stdin: `1000\n10\n1\n1\n` },
  ]}
  hints={[
    'まず小計を求めて変数に入れます。消費税と合計は、その小計から順に求めます。第1章3節（変数に値を残す）を読んでください。',
    '小数点以下を切り捨てるには、整数でない数を `int()` に渡します。第2章1節（intとfloatを使い分ける）を読んでください。',
    '3行の表示は、第1章4節（f文字列で文章にまとめる）の書き方でそろえます。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'round-math', 'print']}
>
パン屋のレシートを作ります。りんごパンと食パンの値段と個数から、小計・消費税・合計を求めてください。

消費税は小計の10%で、1円未満は切り捨てます。合計は小計と消費税を足した金額です。

<Input>
- 1行目: りんごパンの値段（整数）
- 2行目: りんごパンの個数（整数）
- 3行目: 食パンの値段（整数）
- 4行目: 食パンの個数（整数）
</Input>

<Output>
小計・消費税・合計を、この順に3行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-09-29-b5"
  kind="build"
  stdin={`20
30`}
  tests={[
    { kind: 'stdout', stdin: `20\n30\n` },
    { kind: 'stdout', stdin: `15\n15\n` },
    { kind: 'stdout', stdin: `1\n4\n` },
    { kind: 'stdout', stdin: `25\n10\n` },
  ]}
  hints={[
    '円の面積は、半径を2乗した数に円周率をかけると求まります。直径を2で割ると半径になります。',
    '円周率は `math.pi`、2乗は `**` で書きます。第2章5節（mathモジュールを使う）と第2章3節（べき乗と余りを求める）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'round-math', 'operators', 'print']}
>
2枚のピザの直径から、それぞれの面積と、2枚目が1枚目の何倍の面積かを求めてください。面積は小数第1位まで、倍率は小数第2位まで丸めます。

<Input>
- 1行目: 1枚目のピザの直径（cm、整数）
- 2行目: 2枚目のピザの直径（cm、整数）
</Input>

<Output>
1枚目と2枚目の面積を、この順に2行で表示します。続けて、2枚目が1枚目の何倍かを1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-09-29-b6"
  kind="build"
  stdin={`0
0
3
4`}
  tests={[
    { kind: 'stdout', stdin: `0\n0\n3\n4\n` },
    { kind: 'stdout', stdin: `2\n2\n2\n2\n` },
    { kind: 'stdout', stdin: `0\n0\n1\n1\n` },
    { kind: 'stdout', stdin: `-3\n-4\n3\n4\n` },
  ]}
  hints={[
    '2点のx座標どうし、y座標どうしの差を求め、それぞれを2乗して足します。',
    'その和の平方根が距離です。平方根は `math.sqrt()` で求められます。第2章5節（mathモジュールを使う）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'round-math', 'operators', 'print']}
>
2つの点の座標から、そのあいだの距離を求めてください。距離は小数第2位まで丸めます。

<Input>
- 1行目: 1つ目の点のx座標（整数）
- 2行目: 1つ目の点のy座標（整数）
- 3行目: 2つ目の点のx座標（整数）
- 4行目: 2つ目の点のy座標（整数）
</Input>

<Output>
2点のあいだの距離を1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-09-29-b4"
  kind="build"
  stdin={`9
50
15`}
  tests={[
    { kind: 'stdout', stdin: `9\n50\n15\n` },
    { kind: 'stdout', stdin: `23\n30\n45\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n` },
    { kind: 'stdout', stdin: `8\n0\n1500\n` },
  ]}
  hints={[
    '時と分のままだと繰り上がりが面倒です。いったん、0時0分から何分たったかに直してから、かかる時間を足します。',
    '分から「時」と「分」に戻すには `//` と `%` を使います。第2章3節（べき乗と余りを求める）を読んでください。',
    '24時を過ぎたら、24で割った余りで0時から数え直せます。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
待ち合わせの場所に着く時刻を求めてください。

日付をまたいだら0時から数え直します。たとえば23時30分に出発して45分かかると、0時15分に着きます。

<Input>
- 1行目: 出発の時（整数）
- 2行目: 出発の分（整数）
- 3行目: かかる時間（分、整数）
</Input>

<Output>
着く時刻を1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>
