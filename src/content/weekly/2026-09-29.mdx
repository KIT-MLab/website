---
id: weekly-2026-09-29
date: '2026-09-29'
title: 第1〜2章
chapters: ['01-python', '02-numbers']
---

{/* はじめに */}

第1章と第2章から5問です。前半の3問は復習なので、サッと解いてください。後半の2問が今日の本番で、困ったら問題に書いてある節やヒントを使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-09-29-c1"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[
    '入力欄から受け取った値が数か文字列かは、第1章2節（inputで値を受け取る）に書いてあります。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
入力欄に `5` と書いて、次のコードを実行します。表示されるものはどれですか。

```python
x = input()
print(x * 3)
```

- `15`
- `555`
- `5 5 5`
- エラーになる
</Exercise>

<Exercise
  id="weekly-2026-09-29-b1"
  kind="build"
  stdin={`125`}
  tests={[
    { kind: 'stdout', stdin: `125\n` },
    { kind: 'stdout', stdin: `0\n` },
    { kind: 'stdout', stdin: `60\n` },
    { kind: 'stdout', stdin: `3599\n` },
  ]}
  hints={[
    '割り算の商は `//`、余りは `%` で求めます。第2章3節（べき乗と余りを求める）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
入力欄から秒数を整数で読み取ってください。何分何秒かを求め、`125秒は 2分5秒です` の形で表示してください。
</Exercise>

<Exercise
  id="weekly-2026-09-29-b2"
  kind="build"
  stdin={`5
24`}
  tests={[
    { kind: 'stdout', stdin: `5\n24\n` },
    { kind: 'stdout', stdin: `0\n30\n` },
    { kind: 'stdout', stdin: `42.195\n180\n` },
  ]}
  hints={[
    '距離は小数なので、第2章1節（intとfloatを使い分ける）を読んでください。',
    '時速は、距離を「かかった時間（時間）」で割った値です。分を時間に直すには60で割ります。丸め方は第2章2節（round()で桁を整える）にあります。',
  ]}
  mistakes={[]}
  syntax={['input', 'round-math', 'operators', 'print']}
>
入力欄の1行目で走った距離（km、小数）を、2行目でかかった時間（分、整数）を読み取ってください。

時速を小数第1位まで丸めて、`時速 12.5km` の形で表示してください。5kmを24分で走ったときの例です。
</Exercise>

<Exercise
  id="weekly-2026-09-29-b3"
  kind="build"
  stdin={`150
3
400
2`}
  tests={[
    { kind: 'stdout', stdin: `150\n3\n400\n2\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `99\n1\n0\n0\n` },
    { kind: 'stdout', stdin: `1000\n10\n1\n1\n` },
  ]}
  hints={[
    'まず小計を求めて変数に入れます。消費税・合計・ポイントは、その小計や合計から順に求めます。第1章3節（変数に値を残す）を読んでください。',
    '1円未満の切り捨ても、100円ごとのポイントも `//` で求められます。第2章3節（べき乗と余りを求める）を読んでください。',
    '4行の表示は、第1章4節（f文字列で文章にまとめる）の書き方でそろえます。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'round-math', 'print']}
>
パン屋のレシートを作ります。入力欄から、りんごパンの値段・個数、食パンの値段・個数を、この順に1行ずつ整数で読み取ってください。

消費税は小計の10%で、1円未満は切り捨てます。合計は小計と消費税を足した金額です。合計100円ごとに1ポイントが付きます。

りんごパン150円を3個、食パン400円を2個買ったときは、次のように表示します。

```text
小計 1250円
消費税 125円
合計 1375円
ポイント 13
```
</Exercise>

<Exercise
  id="weekly-2026-09-29-b4"
  kind="build"
  stdin={`9
50
15`}
  tests={[
    { kind: 'stdout', stdin: `9\n50\n15\n` },
    { kind: 'stdout', stdin: `23\n30\n45\n` },
    { kind: 'stdout', stdin: `0\n0\n0\n` },
    { kind: 'stdout', stdin: `8\n0\n1500\n` },
  ]}
  hints={[
    '時と分のままだと繰り上がりが面倒です。いったん、0時0分から何分たったかに直してから、かかる時間を足します。',
    '分から「時」と「分」に戻すには `//` と `%` を使います。第2章3節（べき乗と余りを求める）を読んでください。',
    '24時を過ぎたら、24で割った余りで0時から数え直せます。',
  ]}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
待ち合わせの場所に着く時刻を求めます。入力欄から、出発の時・出発の分・かかる時間（分）を、この順に1行ずつ整数で読み取ってください。

9時50分に出発して15分かかるときは、`到着は 10時5分です` と表示してください。日付をまたいだら0時から数え直します。23時30分に出発して45分かかるときは、`到着は 0時15分です` と表示します。
</Exercise>
