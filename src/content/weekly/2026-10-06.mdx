---
id: weekly-2026-10-06
date: '2026-10-06'
title: 第3〜4章と練習編1
chapters: ['00-start', '01-python', '02-numbers', '03-branch', '04-loop', '04p-practice1']
---

{/* はじめに */}

第3章から第4章と、練習編1までの範囲で4問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-10-06-b1"
  kind="build"
  stdin={`10`}
  tests={[
    { kind: 'stdout', stdin: `10\n` },
    { kind: 'stdout', stdin: `12\n` },
    { kind: 'stdout', stdin: `18\n` },
    { kind: 'stdout', stdin: `65\n` },
    { kind: 'stdout', stdin: `0\n` },
  ]}
  hints={[
    '年齢の範囲は、大きいほうから順にelifで確かめると書きやすいです。第3章3節（elifで3段階以上に分ける）を読んでください。',
    '境界の年齢がどちらに入るかは、比べる記号（`<` か `<=`）で決まります。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['if', 'compare', 'input', 'print']}
>
映画館の入場料を、年齢から求めてください。

<Input>
- 1行目: 年齢（整数）
</Input>

<Output>
次の表にしたがって、金額を1行で表示します。

| 年齢 | 表示 |
|---|---|
| 12歳未満 | `800円` |
| 12歳以上18歳未満 | `1200円` |
| 18歳以上65歳未満 | `1800円` |
| 65歳以上 | `1000円` |
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b2"
  kind="build"
  stdin={`3
80
90
70`}
  tests={[
    { kind: 'stdout', stdin: `3\n80\n90\n70\n` },
    { kind: 'stdout', stdin: `1\n80\n` },
    { kind: 'stdout', stdin: `1\n0\n` },
    { kind: 'stdout', stdin: `4\n80\n80\n80\n80\n` },
  ]}
  hints={[
    '数えるための変数を0から用意し、条件に合うたびに1増やします。第4章1節（for文で繰り返す）を読んでください。',
    '80点以上かどうかの比べ方は、第3章1節（if文で条件によって処理を変える）を確認してください。',
  ]}
  mistakes={[]}
  syntax={['for', 'if', 'compare', 'input']}
>
小テストの点数から、80点以上だった科目の数を数えてください。

<Input>
- 1行目: 科目の数（整数）
- 2行目から: 科目の数ぶんの点数（1行に1つ、整数）
</Input>

<Output>
80点以上だった科目の数を1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b3"
  kind="build"
  stdin={`3
60
70
80`}
  tests={[
    { kind: 'stdout', stdin: `3\n60\n70\n80\n` },
    { kind: 'stdout', stdin: `1\n50\n` },
    { kind: 'stdout', stdin: `4\n50\n50\n50\n50\n` },
    { kind: 'stdout', stdin: `2\n0\n100\n` },
  ]}
  hints={[
    '合計を求めてから、人数で割ると平均になります。第2章2節（round()で桁を整える）と第4章2節（リストで値をまとめて持つ）を読んでください。',
    '平均が分かったら、もう一度リストを見て、平均より大きい値の数を数えます。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'list', 'for', 'round-math']}
>
得点の平均と、平均より高い得点を取った人数を求めてください。平均は小数第1位まで丸めます。

<Input>
- 1行目: 人数（整数）
- 2行目から: 人数ぶんの得点（1行に1つ、整数）
</Input>

<Output>
平均と、平均より高い得点を取った人数を、この順に2行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b4"
  kind="build"
  stdin={`1000
-300
500
終わり`}
  tests={[
    { kind: 'stdout', stdin: `1000\n-300\n500\n終わり\n` },
    { kind: 'stdout', stdin: `終わり\n` },
    { kind: 'stdout', stdin: `-100\n-200\n終わり\n` },
    { kind: 'stdout', stdin: `0\n終わり\n` },
  ]}
  hints={[
    '残高を入れる変数と、支出の回数を数える変数を、繰り返しの前に0で用意します。第4章5節（while文で終わりを決めずに繰り返す）を読んでください。',
    '読み取った値が0より小さいかどうかで、支出かどうかを判定します。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['while', 'if', 'compare', 'input']}
>
お小遣いの出入りから、最後の残高と、支出だった回数を求めてください。残高は0円から始めます。

<Input>
- 1行ごとに: お小遣いの出入り（整数）。収入はプラスの数、支出はマイナスの数
- 最後の行: `終わり`
</Input>

<Output>
残高と、支出だった回数を、この順に2行で表示します。形は出力例のとおりです。
</Output>
</Exercise>
