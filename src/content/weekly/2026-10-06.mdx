---
id: weekly-2026-10-06
date: '2026-10-06'
title: 第3〜4章と練習編1
chapters: ['00-start', '01-python', '02-numbers', '03-branch', '04-loop', '04p-practice1']
---

{/* はじめに */}

第3章から第4章と、練習編1までの範囲で6問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-10-06-b1"
  kind="build"
  stdin={`10`}
  tests={[
    { kind: 'stdout', stdin: `10\n` },
    { kind: 'stdout', stdin: `12\n` },
    { kind: 'stdout', stdin: `18\n` },
    { kind: 'stdout', stdin: `65\n` },
    { kind: 'stdout', stdin: `0\n` },
  ]}
  hints={[
    '年齢の範囲は、大きいほうから順にelifで確かめると書きやすいです。第3章3節（elifで3段階以上に分ける）を読んでください。',
    '境界の年齢がどちらに入るかは、比べる記号（`<` か `<=`）で決まります。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['if', 'compare', 'input', 'print']}
>
映画館の入場料を、年齢から求めてください。

<Input>
- 1行目: 年齢（整数）
</Input>

<Output>
次の表にしたがって、金額を1行で表示します。

| 年齢 | 表示 |
|---|---|
| 12歳未満 | `800円` |
| 12歳以上18歳未満 | `1200円` |
| 18歳以上65歳未満 | `1800円` |
| 65歳以上 | `1000円` |
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b5"
  kind="build"
  stdin={`130
15
0`}
  tests={[
    { kind: 'stdout', stdin: `130\n15\n0\n` },
    { kind: 'stdout', stdin: `120\n12\n0\n` },
    { kind: 'stdout', stdin: `110\n20\n1\n` },
    { kind: 'stdout', stdin: `130\n8\n0\n` },
  ]}
  hints={[
    '身長と年齢の条件を `and` でつなげて1つのif文にします。年齢か保護者同伴のどちらかが成り立てばよいところは `or` でつなげます。',
    'and・orの書き方は第3章4節（and・or・notで条件を組み合わせる）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'compare', 'if', 'print']}
>
遊園地の絶叫マシンに乗れるかを判定してください。乗れるのは、身長が120cm以上のときです。ただし、12歳以上か、保護者と一緒にいるかのどちらかも満たす必要があります。

<Input>
- 1行目: 身長（cm、整数）
- 2行目: 年齢（整数）
- 3行目: 保護者と一緒にいるか（1なら一緒にいる、0ならいない）
</Input>

<Output>
乗れるかどうかを1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b6"
  kind="build"
  stdin={`3
5
12
9`}
  tests={[
    { kind: 'stdout', stdin: `3\n5\n12\n9\n` },
    { kind: 'stdout', stdin: `1\n7\n` },
    { kind: 'stdout', stdin: `4\n0\n0\n0\n0\n` },
    { kind: 'stdout', stdin: `5\n-3\n10\n20\n30\n-3\n` },
  ]}
  hints={[
    '読み取った番号を、for文とappendでリストに追加していきます。第4章2節（リストで値をまとめて持つ）と第4章4節（append()とremove()でリストを変える）を読んでください。',
    '最初の値は添字0、最後の値は添字-1で取り出せます。第4章3節（添字とlen()で位置を指定する）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'for', 'list', 'print']}
>
列に並んだ人の整理券番号から、最初の人と最後の人の番号、そして人数を求めてください。

<Input>
- 1行目: 人数（整数）
- 2行目から: 人数ぶんの整理券番号（1行に1つ、整数）
</Input>

<Output>
最初の人の番号、最後の人の番号、人数を、この順に3行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b7"
  kind="build"
  stdin={`3
80
90
70`}
  tests={[
    { kind: 'stdout', stdin: `3\n80\n90\n70\n` },
    { kind: 'stdout', stdin: `1\n0\n` },
    { kind: 'stdout', stdin: `4\n50\n50\n50\n50\n` },
    { kind: 'stdout', stdin: `2\n-10\n10\n` },
  ]}
  hints={[
    '人数ぶんの得点を、for文とappendでリストに読み込みます。第4章1節（for文で繰り返す）と第4章2節（リストで値をまとめて持つ）を読んでください。',
    'リストの値を順に取り出しながら足していくと合計になります。丸め方は第2章2節（round()で桁を整える）にあります。',
  ]}
  mistakes={[]}
  syntax={['input', 'for', 'list', 'round-math', 'print']}
>
人数ぶんの得点から、合計と平均を求めてください。平均は小数第1位まで丸めます。

<Input>
- 1行目: 人数（整数）
- 2行目から: 人数ぶんの得点（1行に1つ、整数）
</Input>

<Output>
合計と平均を、この順に2行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b8"
  kind="build"
  stdin={`10
20
30
40
50
終わり`}
  tests={[
    { kind: 'stdout', stdin: `10\n20\n30\n40\n50\n終わり\n` },
    { kind: 'stdout', stdin: `1\n終わり\n` },
    { kind: 'stdout', stdin: `0\n0\n終わり\n` },
    { kind: 'stdout', stdin: `-5\n-10\n-15\n終わり\n` },
  ]}
  hints={[
    '人数が分からないので、`終わり`が来るまでwhile文で読み取り、リストに追加していきます。第4章5節（while文で終わりを決めずに繰り返す）を読んでください。',
    '全部読み終わってから、len()で人数を求め、その半分の位置を添字にして取り出します。第4章3節（添字とlen()で位置を指定する）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['input', 'while', 'list', 'print']}
>
整理券番号を、`終わり`が来るまで1行に1つずつ読み取ります。真ん中に近い人の番号と、人数を求めてください。真ん中に近い人は、先頭から0、1、2…と数え、人数を2で割った商の位置にいる人と決めます。

<Input>
- 1行ごとに: 整理券番号（整数）
- 最後の行: `終わり`
</Input>

<Output>
真ん中に近い人の番号と、人数を、この順に2行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-06-b4"
  kind="build"
  stdin={`1000
-300
500
終わり`}
  tests={[
    { kind: 'stdout', stdin: `1000\n-300\n500\n終わり\n` },
    { kind: 'stdout', stdin: `終わり\n` },
    { kind: 'stdout', stdin: `-100\n-200\n終わり\n` },
    { kind: 'stdout', stdin: `0\n終わり\n` },
  ]}
  hints={[
    '残高を入れる変数と、支出の回数を数える変数を、繰り返しの前に0で用意します。第4章5節（while文で終わりを決めずに繰り返す）を読んでください。',
    '読み取った値が0より小さいかどうかで、支出かどうかを判定します。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['while', 'if', 'compare', 'input']}
>
お小遣いの出入りから、最後の残高と、支出だった回数を求めてください。残高は0円から始めます。

<Input>
- 1行ごとに: お小遣いの出入り（整数）。収入はプラスの数、支出はマイナスの数
- 最後の行: `終わり`
</Input>

<Output>
残高と、支出だった回数を、この順に2行で表示します。形は出力例のとおりです。
</Output>
</Exercise>
