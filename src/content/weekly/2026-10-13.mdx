---
id: weekly-2026-10-13
date: '2026-10-13'
title: 第5〜6章
chapters: ['00-start', '01-python', '02-numbers', '03-branch', '04-loop', '04p-practice1', '05-function', '06-error']
---

{/* はじめに */}

第5章から第6章までの範囲で4問です。教材をまだ進めていなくても、問題に書いてある節を読めば解けます。困ったら、下に出るヒントも使ってください。

{/* 課題 */}

<Exercise
  id="weekly-2026-10-13-b1"
  kind="build"
  tests={[
    { kind: 'call', fn: 'charge', args: [5] },
    { kind: 'call', fn: 'charge', args: [0] },
    { kind: 'call', fn: 'charge', args: [5, 10] },
    { kind: 'call', fn: 'charge', args: [3, 0] },
  ]}
  hints={[
    '引数に既定値を持たせる書き方は、第5章3節（引数と既定値）を読んでください。',
    '延滞料金は「延滞した分数 × 1分あたりの料金」で求めます。',
  ]}
  mistakes={[]}
  syntax={['function', 'operators']}
>
図書室の本を延滞したときの料金を返す関数 `charge(minutes, rate=20)` を書いてください。

<Input>
関数に渡す引数です。入力欄は使いません。

- `minutes`: 延滞した分数（整数）
- `rate`: 1分あたりの料金（円、整数）。渡さなかったときは `20`
</Input>

<Output>
料金（円）を返します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-13-b2"
  kind="build"
  stdin={`9000
3`}
  tests={[
    { kind: 'stdout', stdin: `9000\n3\n` },
    { kind: 'stdout', stdin: `9000\n0\n` },
    { kind: 'stdout', stdin: `0\n5\n` },
    { kind: 'stdout', stdin: `10000\n3\n` },
  ]}
  hints={[
    '`hours` が `0` かどうかを、関数の中でif文を使って先に確かめます。第3章1節（if文で条件によって処理を変える）と第5章2節（returnで値を持ち帰る）を読んでください。',
    '0で割ろうとするとエラーになることは、第2章3節（べき乗と余りを求める）のよくある間違いで見ました。',
  ]}
  mistakes={[]}
  syntax={['function', 'if', 'compare', 'errors']}
>
1時間あたりの時給を返す関数 `hourly_wage(pay, hours)` を書いてください。`pay` はバイトで受け取った給料、`hours` は働いた時間です。時給は整数に丸めます。

`hours` が `0` のときは、割り算をせずに文字列 `"勤務時間が0です"` を返してください。

<Input>
- 1行目: 給料（整数）
- 2行目: 働いた時間（整数）
</Input>

<Output>
読み取った2つの値を `hourly_wage` に渡して呼び出し、返ってきた値をそのまま1行で表示します。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-13-b3"
  kind="build"
  stdin={`3
5
3
7`}
  tests={[
    { kind: 'stdout', stdin: `3\n5\n3\n7\n` },
    { kind: 'stdout', stdin: `1\n5\n` },
    { kind: 'stdout', stdin: `1\n0\n` },
    { kind: 'stdout', stdin: `4\n5\n5\n5\n5\n` },
  ]}
  hints={[
    '数える処理を関数にまとめ、既定値としきい値を上書きした呼び出しの両方に使います。第5章3節（引数と既定値）と第4章2節（リストで値をまとめて持つ）を読んでください。',
    'for文の中でif文を使って、条件に合うときだけ数を増やします。第4章1節（for文で繰り返す）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['function', 'list', 'for', 'compare']}
>
関数 `heavy_readers(counts, threshold=5)` を書いてください。`counts` は1か月に借りた本の冊数のリストです。冊数が `threshold` 以上だった人数を返します。

<Input>
- 1行目: 人数（整数）
- 2行目から: 人数ぶんの冊数（1行に1つ、整数）
</Input>

<Output>
既定のしきい値（5冊）での人数と、しきい値を `3` にしたときの人数を、この順に2行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="weekly-2026-10-13-b4"
  kind="build"
  stdin={`5
1
3
終わり`}
  tests={[
    { kind: 'stdout', stdin: `5\n1\n3\n終わり\n` },
    { kind: 'stdout', stdin: `終わり\n` },
    { kind: 'stdout', stdin: `0\n終わり\n` },
    { kind: 'stdout', stdin: `3\n3\n終わり\n` },
  ]}
  hints={[
    '1日分の判定を関数にまとめ、while文で終わりまで繰り返し呼び出します。第4章5節（while文で終わりを決めずに繰り返す）と第5章3節（引数と既定値）を読んでください。',
    '「注意」だった日の数を、繰り返しの中で1つずつ数えます。第3章1節（if文で条件によって処理を変える）を読んでください。',
  ]}
  mistakes={[]}
  syntax={['function', 'while', 'compare', 'if']}
>
部活で1日に出た忘れ物の数から、その日を判定する関数 `judge_day(count, limit=3)` を書いてください。`count` は1日分の忘れ物の数、`limit` はしきい値です。

<Input>
- 1行ごとに: 1日分の忘れ物の数（整数）
- 最後の行: `終わり`
</Input>

<Output>
1日分を読み取るたびに `judge_day` を呼び出し、返ってきた言葉を1行ずつ表示します。返す言葉は、しきい値以上なら `"注意"`、そうでなければ `"問題なし"` です。

最後に、`"注意"` だった日数を表示します。形は出力例のとおりです。
</Output>
</Exercise>
