---
id: practice-01-variable
chapter: 01-python
topic: variable
title: 変数
sections: [python-01-variable]
order: 3
levels: [1, 2, 3]
---

<Exercise
  id="practice-01-variable-1"
  kind="build"
  level={1}
  name="長方形の周り"
  stdin={`3
5`}
  tests={[
    { kind: 'stdout', stdin: `3\n5\n` },
    { kind: 'stdout', stdin: `0\n7\n` },
    { kind: 'stdout', stdin: `4\n4\n` },
  ]}
  hints={['読み取った値を変数に入れておくと、同じ値を何度でも使えます。第1章3節を見てください。']}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
長方形の縦と横の長さから、周りの長さを求めてください。

<Input>
- 1行目: 縦の長さ（cm、整数）
- 2行目: 横の長さ（cm、整数）
</Input>

<Output>
周りの長さを、数だけ1行で表示します。
</Output>
</Exercise>

<Exercise
  id="practice-01-variable-2"
  kind="build"
  level={1}
  name="和と差"
  stdin={`10
3`}
  tests={[
    { kind: 'stdout', stdin: `10\n3\n` },
    { kind: 'stdout', stdin: `0\n0\n` },
    { kind: 'stdout', stdin: `-2\n5\n` },
  ]}
  hints={['2つの値を、それぞれ別の名前の変数に入れてから計算します。']}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
2つの整数の和と差を求めてください。差は、1つ目から2つ目を引いた値です。

<Input>
- 1行目: 1つ目の整数
- 2行目: 2つ目の整数
</Input>

<Output>
和を1行目、差を2行目に表示します。
</Output>
</Exercise>

<Exercise
  id="practice-01-variable-3"
  kind="build"
  level={2}
  name="値上げの前と後"
  stdin={`120
30
4`}
  tests={[
    { kind: 'stdout', stdin: `120\n30\n4\n` },
    { kind: 'stdout', stdin: `100\n0\n3\n` },
    { kind: 'stdout', stdin: `250\n50\n0\n` },
  ]}
  hints={['値上げしたあとの値段を、先に別の変数に入れておきます。', '2つの代金も変数に入れておくと、差の計算にそのまま使えます。']}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
ある品物が値上げされます。値上げの前と後で、同じ個数を買ったときの代金を求めてください。続けて、代金がいくら増えるかも求めます。

<Input>
- 1行目: 値上げ前の値段（円、整数）
- 2行目: 値上げの額（円、整数）
- 3行目: 買う個数（整数）
</Input>

<Output>
値上げ前の代金、値上げ後の代金、増えた額を、この順に3行で表示します。数だけを表示します。
</Output>
</Exercise>

<Exercise
  id="practice-01-variable-4"
  kind="build"
  level={2}
  name="アルバイトの給料"
  stdin={`1100
4
10`}
  tests={[
    { kind: 'stdout', stdin: `1100\n4\n10\n` },
    { kind: 'stdout', stdin: `1200\n0\n5\n` },
    { kind: 'stdout', stdin: `1000\n8\n1\n` },
  ]}
  hints={['先に1日の給料を求めて変数に入れます。期間全体の給料は、その変数から求めます。']}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
時給と、1日に働く時間と、働く日数から、1日の給料と、期間全体の給料を求めてください。

<Input>
- 1行目: 時給（円、整数）
- 2行目: 1日に働く時間（時間、整数）
- 3行目: 働く日数（整数）
</Input>

<Output>
1日の給料と、期間全体の給料を、この順に2行で表示します。数だけを表示します。
</Output>
</Exercise>

<Exercise
  id="practice-01-variable-5"
  kind="build"
  level={3}
  name="動物園の入場料"
  stdin={`2
3`}
  tests={[
    { kind: 'stdout', stdin: `2\n3\n` },
    { kind: 'stdout', stdin: `0\n0\n` },
    { kind: 'stdout', stdin: `1\n1\n` },
  ]}
  hints={['大人の料金と子どもの料金を、それぞれ変数に入れておきます。合計はその2つから求めます。', '1行に文字列と数を並べる書き方は、第1章1節にあります。']}
  mistakes={[]}
  syntax={['input', 'operators', 'print']}
>
動物園の入場料は、大人1人1500円、子ども1人600円です。大人と子どもの人数から、大人の料金・子どもの料金・合計を求めてください。

<Input>
- 1行目: 大人の人数（整数）
- 2行目: 子どもの人数（整数）
</Input>

<Output>
1行目から順に `大人`・`子ども`・`合計` と書き、それぞれ空白をはさんで金額を表示します。形は出力例のとおりです。
</Output>
</Exercise>
