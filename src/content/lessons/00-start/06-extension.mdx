---
id: start-06-extension
chapter: 00-start
title: 拡張子
minutes: 8
terms: [拡張子]
---

{/* 困る例 */}

ファイル名が「資料」とだけ見えると、文章か写真か、開く道具が分かりません。種類を表す文字列は、既定では隠れています。

{/* やってみる */}

<Exercise
  id="start-06-extension-1"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[]}
  mistakes={[]}
>
エクスプローラーで、好きなファイルを1つ見てください。いま、ファイル名の末尾に何が見えますか。

- `.txt` や `.jpg` のような文字列が見える
- 何も見えない
- 作った日付が見える
- ファイルの保存場所が見える
</Exercise>

{/* 説明 */}

ファイル名の末尾には、種類を表す短い文字列が付きます。これを拡張子と呼びます。多くのパソコンは、既定でこれを隠しています。

表示する設定に変えると、`.py` や `.jpg` のような文字列が見えるようになります。`.py` は、次の章から作るものです。

<Level0 title="拡張子を表示する設定">
エクスプローラーの上のほうにある「表示」を選びます。一覧の中から、「ファイル名拡張子」にチェックを入れてください。

チェックすると、ファイル名の後ろに `.txt` や `.jpg` のような文字列が見えるようになります。
</Level0>

{/* 課題 */}

<Exercise
  id="start-06-extension-2"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[]}
  mistakes={[]}
>
設定を変えたあと、ファイル名の末尾には何が見えますか。

- 作った日付
- ファイルの種類を表す短い文字列
- ファイルの大きさ
</Exercise>

<Exercise
  id="start-06-extension-3"
  kind="choose"
  tests={[{ kind: 'choice', correct: 2 }]}
  hints={[]}
  mistakes={[]}
>
写真のファイルによく使われる拡張子はどれですか。

- `.docx`
- `.jpg`
- `.mp3`
- `.zip`
</Exercise>

<Exercise
  id="start-06-extension-4"
  kind="choose"
  tests={[{ kind: 'choice', correct: 1 }]}
  hints={[]}
  mistakes={[]}
>
次の章から作るファイルに付く拡張子はどれですか。

- `.py`
- `.jpg`
- `.mp3`
- `.pdf`
</Exercise>

<Exercise
  id="start-06-extension-5"
  kind="choose"
  tests={[{ kind: 'choice', correct: 1 }]}
  hints={[]}
  mistakes={[]}
>
拡張子の説明として、正しいものはどれですか。

- ファイル名の末尾に付く、種類を表す文字列
- ファイルを保存した日付
- ファイルの中身をそのまま表示したもの
- フォルダの中身の数
</Exercise>

{/* つながり */}

次の章から、実際に Python を書きはじめます。ここまでの操作が、そのまま道具になります。
