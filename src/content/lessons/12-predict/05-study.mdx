---
id: python-12-study
chapter: 12-predict
title: 勉強時間と点数から予想する
minutes: 20
terms: []
---

{/* 困る例 */}

ここまでは数人だけで確かめてきました。30人分のデータ全部を使って、いちばん当てはまる直線を選び、勉強時間5時間の新しい人の点数を予想したいです。

{/* やってみる */}

次のコードの ▶ を押してください。

<Run
  code={`import numpy as np

hours = np.array([2.0, 3.0, 5.0, 6.0, 8.0, 9.0, 6.0, 6.5, 8.5, 5.5, 7.0, 8.0, 2.5, 0.5, 3.0, 3.0, 8.0, 8.5, 0.5, 4.5, 7.5, 1.5, 7.5, 1.5, 4.5, 7.5, 3.0, 3.5, 3.0, 6.5])
scores = np.array([45, 52, 65, 70, 82, 84, 63, 70, 85, 68, 70, 80, 44, 39, 62, 52, 85, 91, 33, 66, 80, 39, 77, 39, 72, 73, 52, 50, 49, 77])

def loss(a, b):
    pred = a * hours + b
    return np.mean((pred - scores) ** 2)

a_candidates = np.linspace(5.0, 7.0, 21)
b_candidates = np.linspace(30.0, 36.0, 61)

best_a = a_candidates[0]
best_b = b_candidates[0]
best_loss = loss(best_a, best_b)
for a in a_candidates:
    for b in b_candidates:
        current = loss(a, b)
        if current < best_loss:
            best_loss = current
            best_a = a
            best_b = b

new_hours = 5
print(round(best_a, 2))
print(round(best_b, 2))
print(round(best_loss, 2))
print(round(best_a * new_hours + best_b, 1))`}
  out={`6.1
33.1
19.03
63.6`}
/>

{/* 説明 */}

3〜4行目の `hours` と `scores` は、この章で使ってきたデータの30人分です。10〜22行目は、第12章3節と同じ候補を全部試す方法で、損失が最小の `a` と `b` を選びます。

25〜27行目の `6.1`、`33.1`、`19.03` が、選ばれた直線とその損失です。28行目は、勉強時間5時間の新しい人の点数を、選んだ直線で予想しています。

この `a` と `b` を、第13章ではもっと効率よく求め直します。

{/* 課題 */}

<Exercise
  id="python-12-study-b1"
  kind="build"
  stdin={`3
5
0
9.5`}
  tests={[
    { kind: 'stdout', stdin: `3\n5\n0\n9.5\n` },
    { kind: 'stdout', stdin: `1\n2.5\n` },
    { kind: 'stdout', stdin: `2\n4\n4\n` },
  ]}
  hints={[
    'データと候補の作り方、2重のfor文は、上の「やってみる」と同じ形です。',
    '新しい人の勉強時間を人数ぶん読み取る形は、第7章5節と同じです。',
    '選んだ直線での予想は、best_a * 勉強時間 + best_b です。',
  ]}
  mistakes={[]}
>
上の「やってみる」と同じ30人のデータを使います。候補も同じで、`a` は5.0から7.0までの21個、`b` は30.0から36.0までの61個です。この候補から、損失が最小の `a` と `b` を選んでください。

入力欄の1行目を新しい人の人数として読み取り、続けて人数ぶんの勉強時間を読み取ってください。

選んだ `a`、`b`、損失を小数第2位まで丸めて、この順に表示してください。続けて、新しい人の予想の点数を、入力した順に1人1行で、小数第1位まで丸めて表示してください。
</Exercise>

{/* つながり */}

候補を全部試して、勉強時間と点数から新しい人を予想できました。次の章では、この `a` と `b` をもっと効率よく求めます。
