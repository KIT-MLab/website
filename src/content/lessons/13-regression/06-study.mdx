---
id: python-13-study
chapter: 13-regression
title: 勉強時間・睡眠時間から点数を予想する
minutes: 25
terms: []
---

{/* 困る例 */}

第12章5節は、勉強時間だけの直線を候補の総当たりで選びました。睡眠時間も使うと候補はさらに増え（第12章4節）、総当たりでは重すぎます。

{/* やってみる */}

次のコードの ▶ を押してください。

<Run
  code={`import numpy as np

hours = np.array([2.0, 3.0, 5.0, 6.0, 8.0, 9.0, 6.0, 6.5, 8.5, 5.5, 7.0, 8.0, 2.5, 0.5, 3.0, 3.0, 8.0, 8.5, 0.5, 4.5, 7.5, 1.5, 7.5, 1.5, 4.5, 7.5, 3.0, 3.5, 3.0, 6.5])
sleep = np.array([6.5, 7.0, 6.0, 6.5, 7.0, 5.5, 8.5, 6.5, 6.5, 6.5, 7.0, 6.5, 6.5, 8.5, 8.0, 8.0, 7.5, 7.0, 6.0, 8.5, 6.5, 5.0, 8.0, 5.0, 8.0, 7.0, 5.0, 4.5, 6.5, 4.5])
scores = np.array([45, 52, 65, 70, 82, 84, 63, 70, 85, 68, 70, 80, 44, 39, 62, 52, 85, 91, 33, 66, 80, 39, 77, 39, 72, 73, 52, 50, 49, 77])

h_mean = hours.mean()
h_std = hours.std()
s_mean = sleep.mean()
s_std = sleep.std()
X = np.array([(hours - h_mean) / h_std, (sleep - s_mean) / s_std]).T

def loss(w, b):
    pred = X @ w + b
    return np.mean((pred - scores) ** 2)

def grad_w(w, b):
    pred = X @ w + b
    n = len(scores)
    return X.T @ (pred - scores) * 2 / n

def grad_b(w, b):
    pred = X @ w + b
    return np.mean(2 * (pred - scores))

w = np.array([0.0, 0.0])
b = 0.0
loss_before = loss(w, b)

for i in range(200):
    gw = grad_w(w, b)
    gb = grad_b(w, b)
    w = w - 0.1 * gw
    b = b - 0.1 * gb

new_x = np.array([(5 - h_mean) / h_std, (7 - s_mean) / s_std])
print(round(loss_before, 2))
print(round(loss(w, b), 2))
print(np.round(w, 2))
print(round(b, 2))
print(round(new_x @ w + b, 1))`}
  out={`4334.2
16.88
[15.51  1.47]
63.8
64.0`}
/>

{/* 説明 */}

7〜11行目で勉強時間と睡眠時間を標準化し（第13章5節）、1人ぶんが1行になる `X` を作ります。30〜34行目の勾配降下は、第13章4節と同じ式です。

学習前の損失（`4334.2`）と学習後の損失（`16.88`）を比べると、200回のくり返しで大きく下がったと分かります。36行目以降は、勉強時間5時間・睡眠時間7時間の人の予想です。

{/* 課題 */}

<Exercise
  id="python-13-study-b1"
  kind="build"
  stdin={`2
5
7
8
6`}
  tests={[
    { kind: 'stdout', stdin: `2\n5\n7\n8\n6\n` },
    { kind: 'stdout', stdin: `0\n` },
    { kind: 'stdout', stdin: `1\n0\n6.5\n` },
  ]}
  hints={[
    '30人のデータ、標準化、loss・grad の式は、上の「やってみる」と同じものです。',
    '学習率0.1で200回くり返すところまでは、やってみる と同じです。',
  ]}
  mistakes={[]}
>
30人ぶんの勉強時間・睡眠時間・点数から、標準化した入力で `w = [0, 0]`、`b = 0` を学習します。学習率は0.1、くり返しは200回です。

学習前の損失を小数第2位まで丸めて1行目に、学習後の損失を小数第2位まで丸めて2行目に表示してください。続けて、学習した `w` を小数第2位まで丸めて配列のまま3行目に、`b` を小数第2位まで丸めて4行目に表示してください。

最後に、入力欄の1行目を新しい人の人数として読み取り、勉強時間・睡眠時間をこの順に人数ぶん読み取ってください。学習に使った平均・標準偏差で標準化して予想し、1人1行で小数第1位まで丸めて表示してください。
</Exercise>

{/* つながり */}

候補の総当たりを勾配降下に置き換え、睡眠時間も使えるようになりました。次の章では、数を当てるのではなく種類を当てる分類を扱います。
