---
id: python-01-fstring
chapter: 01-python
title: f文字列で文章にまとめる
minutes: 14
terms: [f文字列, str]
---

{/* 困る例 */}

名前と点数を1行にまとめて表示したいとします。`"点数は" + 80` のように文字列と数を `+` でつなぐと、Pythonはエラーで止まり、報告の文が作れません。

{/* やってみる */}

次のコードの ▶ を押してください。

<Run
  code={`name = "佐藤"
score = 80
print(f"名前:{name} 点数:{score}")`}
  out={`名前:佐藤 点数:80`}
/>

{/* 説明 */}

文字列の前に `f` を付けたものをf文字列（f-string）と呼びます。`{name}` のように `{}` の中に変数を書くと、その値が文字列の中に展開されます。

1行目の `{name}` には `"佐藤"` が、`{score}` には `80` が入ります。数値も `{}` の中に書けば、そのまま文字の並びとして出ます。

`{}` の中には、変数だけでなく計算の式も書けます。`{100 - score}` のように書くと、計算した結果がそのまま文字列に展開されます。

`+` で数値を文字列とつなぐときは、先に `str()` で文字列に変えます。`"点数は" + str(80)` は `"点数は80"` になります。f文字列を使えば、この `str()` を書かずに済みます。

{/* よくある間違い */}

<Mistake id="str-plus-int-literal" code={`print("点数は" + 80)`} error={`TypeError: can only concatenate str (not "int") to str`}>
文字列と数値は `+` で直接つなげません。`str(80)` のように文字列に変えるか、f文字列を使ってください。
</Mistake>

<Mistake id="fstring-missing-brace" code={`name = "佐藤"
print(f"名前:{name")`} error={`SyntaxError: f-string: expecting '}'`}>
`{` に対応する `}` が抜けています。f文字列の `{}` は開いたら必ず閉じてください。
</Mistake>

{/* 課題 */}

<Exercise
  id="python-01-fstring-m1"
  kind="modify"
  starter={`name = "鈴木"
score = 95
print(f"名前:{name} 点数:{score}")`}
  tests={[{ kind: 'stdout' }]}
  hints={[]}
  mistakes={['fstring-missing-brace']}
>
上のコードを書き換えて、`点数:95` の後ろに `点` を付けて `点数:95点` と表示してください。
</Exercise>

<Exercise
  id="python-01-fstring-m2"
  kind="modify"
  starter={`name = "鈴木"
score = 95
print(f"名前:{name} 点数:{score}点")`}
  tests={[{ kind: 'stdout' }]}
  hints={[]}
  mistakes={['fstring-missing-brace']}
>
上のコードを書き換えて、`点数:95点 名前:鈴木` のように、点数を先に、名前をあとに表示してください。
</Exercise>

<Exercise
  id="python-01-fstring-b1"
  kind="build"
  stdin={`田中
80`}
  tests={[
    { kind: 'stdout', stdin: `田中\n80\n` },
    { kind: 'stdout', stdin: `佐藤\n0\n` },
    { kind: 'stdout', stdin: `山田\n-10\n` },
    { kind: 'stdout', stdin: `鈴木\n100\n` },
  ]}
  hints={[
    '1行目を `input()` で読み取り、そのまま名前として使います。',
    '2行目は `int(input())` で点数に変えます。',
    '上の「説明」にある、`{}` に計算の式を書けることを思い出してください。',
  ]}
  mistakes={['str-plus-int-literal']}
>
名前と点数を読み取り、100点まであと何点かを求めてください。

<Input>
- 1行目: 名前
- 2行目: 点数（整数）
</Input>

<Output>
名前・点数・100点まであと何点かを1行で表示します。形は出力例のとおりです。
</Output>
</Exercise>

<Exercise
  id="python-01-fstring-b2"
  kind="build"
  stdin={`りんご`}
  tests={[
    { kind: 'stdout', stdin: `りんご\n` },
    { kind: 'stdout', stdin: `0\n` },
    { kind: 'stdout', stdin: `-5\n` },
    { kind: 'stdout', stdin: `\n` },
  ]}
  hints={[
    '上の「説明」にある、文字列を `+` でつなぐ書き方を、もう一度読んでください。',
    'コンマで区切って `print` に渡すと、間に空白が入ります。1行だけ実行して確かめてください。',
  ]}
  mistakes={['str-plus-int-literal', 'fstring-missing-brace']}
>
入力欄の1行をそのまま読み取り、かっこで囲んで表示するプログラムを書いてください。

<Input>
- 1行目: 文字（何が来てもよい。空の行のこともある）
</Input>

<Output>
読み取った1行の前後を `【` と `】` で囲んで、1行で表示します。
</Output>
</Exercise>

{/* つながり */}

文字列と数値を組み合わせて表示できるようになりました。次の節では、ここまでに習った書き方で点数表を作ります。
