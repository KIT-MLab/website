---
id: python-14-label
chapter: 14-classify
title: 合格の割合を確率とみなす
minutes: 15
terms: [ラベル]
---

{/* 困る例 */}

勉強時間から、合格するかどうかを予想したいです。第13章のように、合格を1、不合格を0とした値に直線を合わせても、出てくる予測は0や1からずれた中途半端な数になります。

{/* やってみる */}

次のコードの ▶ を押してください。

<Run
  code={`import numpy as np

hours = np.array([2.0, 3.0, 5.0, 6.0, 8.0, 9.0, 6.0, 6.5, 8.5, 5.5, 7.0, 8.0, 2.5, 0.5, 3.0, 3.0, 8.0, 8.5, 0.5, 4.5, 7.5, 1.5, 7.5, 1.5, 4.5, 7.5, 3.0, 3.5, 3.0, 6.5])
scores = np.array([45, 52, 65, 70, 82, 84, 63, 70, 85, 68, 70, 80, 44, 39, 62, 52, 85, 91, 33, 66, 80, 39, 77, 39, 72, 73, 52, 50, 49, 77])
passed = (scores >= 60) * 1

a = 0.0
b = 0.0
for i in range(3000):
    z = a * hours + b
    a = a - 0.01 * np.mean(2 * (z - passed) * hours)
    b = b - 0.01 * np.mean(2 * (z - passed))

z = a * hours + b
print(round(z.min(), 2), round(z.max(), 2))`}
  out={`-0.08 1.26`}
/>

{/* 説明 */}

5行目の `(scores >= 60) * 1` は、真偽の配列に1を掛け、合格を1、不合格を0にした整数の配列を作っています。この0と1の値をラベル(label)と呼びます。

9〜12行目は、第13章3節と同じ形の勾配降下法で、`hours` からラベルを予想する直線を合わせています。

15行目の出力を見ると、この直線の予測 `z` は `-0.08` から `1.26` まで広がっています。ラベルは0か1しかないのに、予測はその範囲をはみ出し、確率としては読めません。

{/* 課題 */}

<Exercise
  id="python-14-label-m1"
  kind="modify"
  starter={`import numpy as np

hours = np.array([2.0, 3.0, 5.0, 6.0, 8.0, 9.0, 6.0, 6.5, 8.5, 5.5, 7.0, 8.0, 2.5, 0.5, 3.0, 3.0, 8.0, 8.5, 0.5, 4.5, 7.5, 1.5, 7.5, 1.5, 4.5, 7.5, 3.0, 3.5, 3.0, 6.5])
scores = np.array([45, 52, 65, 70, 82, 84, 63, 70, 85, 68, 70, 80, 44, 39, 62, 52, 85, 91, 33, 66, 80, 39, 77, 39, 72, 73, 52, 50, 49, 77])
passed = (scores >= 60) * 1

a = 0.0
b = 0.0
for i in range(3000):
    z = a * hours + b
    a = a - 0.01 * np.mean(2 * (z - passed) * hours)
    b = b - 0.01 * np.mean(2 * (z - passed))

z = a * hours + b
print(round(z.min(), 2), round(z.max(), 2))`}
  tests={[{ kind: 'stdout' }]}
  hints={[
    'まず、いまのコードをそのまま実行して、出力を確かめてください。',
    'くり返す回数を減らすと、a と b はまだ動く途中の値で止まります。',
  ]}
  mistakes={[]}
>
`-0.06 1.25` と表示されるように、9行目の `range` の引数を書き換えてください。
</Exercise>

<Exercise
  id="python-14-label-b1"
  kind="build"
  stdin={`10
2.0
45
3.0
52
5.0
65
6.0
70
8.0
82
9.0
84
6.0
63
6.5
70
8.5
85
5.5
68`}
  tests={[
    {
      kind: 'stdout',
      stdin: `10\n2.0\n45\n3.0\n52\n5.0\n65\n6.0\n70\n8.0\n82\n9.0\n84\n6.0\n63\n6.5\n70\n8.5\n85\n5.5\n68\n`,
    },
    { kind: 'stdout', stdin: `5\n2.0\n45\n3.0\n52\n5.0\n65\n6.0\n70\n8.0\n82\n` },
    {
      kind: 'stdout',
      stdin: `15\n2.0\n45\n3.0\n52\n5.0\n65\n6.0\n70\n8.0\n82\n9.0\n84\n6.0\n63\n6.5\n70\n8.5\n85\n5.5\n68\n7.0\n70\n8.0\n80\n2.5\n44\n0.5\n39\n3.0\n62\n`,
    },
  ]}
  hints={[
    '第11章1節と同じように、区間ごとに人数と合格した人数を数えます。',
    'if / elif / else で3つの区間に分け、区間ごとに数えたあと、合格した人数を人数で割ります。',
  ]}
  mistakes={[]}
>
入力欄の1行目を人数として読み取ってください。続けてその人数ぶん、勉強時間と点数をこの順に1行ずつ読み取ってください。点数が60点以上なら合格とします。

勉強時間を3時間未満・3時間以上6時間未満・6時間以上の3つの区間に分けます。それぞれの区間で、合格の割合を小数第2位まで丸めて、1行ずつ表示してください。
</Exercise>

{/* つながり */}

予測の値が確率として読めないことが分かりました。次の節では、この値を確率に直す方法を扱います。
