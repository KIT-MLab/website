---
id: python-08p-combine
chapter: 08p-practice2
title: 組み合わせる
minutes: 45
terms: []
---

{/* はじめに */}

ここまでに習った道具だけで解けます。どれを使うかは自分で決めてください。困ったら、下のヒントを開いてください。

{/* 課題 */}

<Exercise
  id="python-08p-combine-b1"
  kind="build"
  stdin={`5
8000
6000
10000
7500
9000`}
  tests={[
    { kind: 'stdout', stdin: `5\n8000\n6000\n10000\n7500\n9000\n` },
    { kind: 'stdout', stdin: `1\n8000\n` },
    { kind: 'stdout', stdin: `3\n0\n8000\n15000\n` },
    { kind: 'stdout', stdin: `2\n9000\n9000\n` },
  ]}
  hints={[
    '配列に条件を付けると、条件に合う要素だけからなる配列になります。その個数を数えれば、目標以上だった日数です。既定の目標のときと、別の目標を指定したときの、両方の日数を同じ関数の呼び出しで求めます。',
    '[第5.4節 名前を書いて引数を渡す](05-function/04-keyword) と [第7.4節 条件やスライスで取り出す](07-array/04-select) を参考にしてください。',
  ]}
>
入力欄の1行目を日数として読み取ってください。続けて、日数ぶんの歩数を1行ずつ読み取ってください。

目標の歩数以上だった日数を返す関数 `goal_days(steps, goal=8000)` を書いてください。`steps` は配列です。

既定の目標での日数を `8000歩以上 3日` の形で表示してください。続けて、`goal=10000` として呼び出したときの日数を `10000歩以上 1日` の形で表示してください。
</Exercise>

<Exercise
  id="python-08p-combine-b2"
  kind="build"
  stdin={`1000
1000
1000
終わり`}
  tests={[
    { kind: 'stdout', stdin: `1000\n1000\n1000\n終わり\n` },
    { kind: 'stdout', stdin: `終わり\n` },
    { kind: 'stdout', stdin: `0\n終わり\n` },
    { kind: 'stdout', stdin: `1200\n850\n終わり\n` },
  ]}
  hints={[
    '終わり になるまで1行ずつ読み取りながら、税込みにした値段をそのつど足していきます。税込みの計算は関数にまとめ、税率は既定値のまま呼び出します。',
    '[第4.5節 while文](04-loop/05-while) と [第5.3節 引数と既定値](05-function/03-args) を参考にしてください。',
  ]}
>
入力欄の値段を1行ずつ、`終わり` になるまで読み取ってください。

税込みの値段を整数に丸めて返す関数 `with_tax(price, rate=0.1)` を書いてください。読み取った値段をすべて税込みにした合計を `合計 3300円` の形で表示してください。
</Exercise>

<Exercise
  id="python-08p-combine-b3"
  kind="build"
  stdin={`3
5000
6000
7000
8000
9000
8000
1000
2000
3000`}
  tests={[
    { kind: 'stdout', stdin: `3\n5000\n6000\n7000\n8000\n9000\n8000\n1000\n2000\n3000\n` },
    { kind: 'stdout', stdin: `1\n3000\n4000\n5000\n` },
    { kind: 'stdout', stdin: `2\n1000\n1000\n1000\n2000\n500\n500\n` },
    { kind: 'stdout', stdin: `2\n0\n0\n0\n1\n1\n1\n` },
  ]}
  hints={[
    '人数行3列の表にしてから、軸を指定した合計でそれぞれの3日合計を求めます。いちばん大きい合計を持つ人の位置を、変数に覚えながらfor文とif文で探します。同じ合計なら、先に見つかったほうを残します。',
    '[第8.5節 reshapeで表にする](08-table/05-reshape) と [第7.3節 平均・合計・最大と軸](07-array/03-stats) を参考にしてください。',
  ]}
>
入力欄の1行目を人数として読み取ってください。続けて、1人ずつ3日ぶんの歩数を1行に1つずつ読み取ります。

3日の合計がいちばん多い人を `いちばん歩いたのは 2人目 25000歩` の形で表示してください。同じ合計の人がいれば、先の人を出してください。
</Exercise>

<Exercise
  id="python-08p-combine-b4"
  kind="build"
  stdin={`2
32
28
30
25
31
31
31
31`}
  tests={[
    { kind: 'stdout', stdin: `2\n32\n28\n30\n25\n31\n31\n31\n31\n` },
    { kind: 'stdout', stdin: `1\n29\n29\n29\n29\n` },
    { kind: 'stdout', stdin: `1\n30\n30\n30\n30\n` },
    { kind: 'stdout', stdin: `1\n-5\n0\n35\n29\n` },
  ]}
  hints={[
    '日数行4列の表にしてから、行の添字で1日ずつ取り出します。取り出した1日ぶんに条件を付ければ、30度以上だった回数を数えられます。',
    '[第8.1節 行と列を指定する添字](08-table/01-index) と [第7.4節 条件やスライスで取り出す](07-array/04-select) を参考にしてください。',
  ]}
>
入力欄の1行目を日数として読み取ってください。続けて、1日ずつ朝・昼・夕・夜の4回の気温を1行に1つずつ読み取ります。

日ごとに、30度以上だった回数を `1日目 2回` の形で1行ずつ表示してください。
</Exercise>

{/* つながり */}

次の練習2-2では、道具の組み合わせ方も自分で選びます。
