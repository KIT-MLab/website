---
id: python-04p-combine
chapter: 04p-practice1
title: 組み合わせる
minutes: 40
terms: []
---

{/* はじめに */}

ここまでに習った道具だけで解けます。どれを使うかは自分で決めてください。困ったら、下のヒントを開いてください。

{/* 課題 */}

<Exercise
  id="python-04p-combine-b1"
  kind="build"
  stdin={`1200
800
1000
終わり`}
  tests={[
    { kind: 'stdout', stdin: `1200\n800\n1000\n終わり\n` },
    { kind: 'stdout', stdin: `終わり\n` },
    { kind: 'stdout', stdin: `500\n300\n999\n終わり\n` },
    { kind: 'stdout', stdin: `0\n0\n2000\n終わり\n` },
  ]}
  hints={[
    '個数と合計を、それぞれ0から始める変数として先に用意します。値段を読み取るたびに、その2つを更新します。',
    '[第4.5節 while文](04-loop/05-while) を参考にしてください。',
  ]}
>
入力欄の値段を1行ずつ、`終わり` になるまで読み取ってください。

1000円以上だった品の個数を `1000円以上 2個` の形で表示してください。続けて、読み取った値段の合計を `合計 3000円` の形で表示してください。
</Exercise>

<Exercise
  id="python-04p-combine-b2"
  kind="build"
  stdin={`10`}
  tests={[
    { kind: 'stdout', stdin: `10\n` },
    { kind: 'stdout', stdin: `21\n` },
    { kind: 'stdout', stdin: `1\n` },
    { kind: 'stdout', stdin: `0\n` },
  ]}
  hints={[
    '1つの数が3の倍数か7の倍数かは、割り算の余りで確かめられます。',
    '[第2.3節 べき乗と余りを求める](02-numbers/03-operators) を参考にしてください。',
  ]}
>
入力欄の1行目を `n` として読み取ってください。`1` から `n` までのうち、3の倍数または7の倍数がいくつあるかを `4個` の形で表示するプログラムを書いてください。
</Exercise>

<Exercise
  id="python-04p-combine-b3"
  kind="build"
  stdin={`5
15
18
17
20
20`}
  tests={[
    { kind: 'stdout', stdin: `5\n15\n18\n17\n20\n20\n` },
    { kind: 'stdout', stdin: `1\n20\n` },
    { kind: 'stdout', stdin: `4\n10\n10\n10\n10\n` },
    { kind: 'stdout', stdin: `3\n-5\n-2\n-10\n` },
  ]}
  hints={[
    '前の日の気温と比べるには、1つ前の添字の値を使います。',
    '[第4.3節 添字とlen()](04-loop/03-index) を参考にしてください。',
  ]}
>
入力欄の1行目を日数として読み取ってください。続けて、日数ぶんの気温を1行ずつ読み取ってください。

前の日より気温が高かった日が何日あったかを `上がった日 2日` の形で表示するプログラムを書いてください。1日目は前の日がないので数えません。
</Exercise>

<Exercise
  id="python-04p-combine-b4"
  kind="build"
  stdin={`150
99
251
200
終わり`}
  tests={[
    { kind: 'stdout', stdin: `150\n99\n251\n200\n終わり\n` },
    { kind: 'stdout', stdin: `100\n250\n終わり\n` },
    { kind: 'stdout', stdin: `180\n終わり\n` },
    { kind: 'stdout', stdin: `0\n-5\n120\n終わり\n` },
  ]}
  hints={[
    '受け付けた値だけをリストに集めておくと、あとでまとめて平均を計算できます。',
    '[第3.4節 and・or・not](03-branch/04-andor) を参考にしてください。',
  ]}
>
入力欄の身長（cm）を1行ずつ、`終わり` になるまで読み取ってください。

`100` 以上 `250` 以下の値だけを受け付けてください。それ以外の値が来たら、その場で `範囲外` と表示し、数えません。

最後に、受け付けた値の平均を小数第1位まで `平均 175.0cm` の形で表示するプログラムを書いてください。
</Exercise>

{/* つながり */}

次の練習1-2では、道具の組み合わせ方も自分で選びます。
