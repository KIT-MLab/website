# デザイン決定記録

ユーザーが選択・決定したデザイン事項の記録。Claude はここに書かれた決定に従う。
新しい決定が下されたら、このファイルに日付付きで追記する。

## 決定済み

### 2026-09-02 進め方
- デザインは「Claude が複数案提示 → ユーザーが選択」の反復で決める
- 全体コンセプト程度の一様さは許容。ただし個別要素の裁量は最小化する

### 2026-09-02 選択の単位
- デザインの選択は「ページ丸ごとの案」ではなく**部品レベル**（書体・色・罫線・ボタン等を個別に）で行う
- 選択ツール: `public/design-explorations/picker.html`（部品ピッカー）
- 方向性の消去法: 案B（ベント/深緑プロダクト風）は「AIっぽい」ため却下、案C（丸ゴシック/カード）は「幼稚っぽい」ため却下。**A（紀要）/ D（実験ノート）の系統で進める**

### 2026-09-02 進め方の補足（ユーザーによる部分撤回）
- セット形式（完成案の複数提示）も「大きな方向性を決める」用途では有効と再評価。以後は **大方針=セット提示 / 細部=部品ピッカー** の二段構えで使い分ける
- デザインは固定ではなく、少しずつ変化していく前提でよい
- キーワード: **シンプルな感じが良いのかも**（ユーザー談）

### 2026-09-02 第1次デザイントークン決定（部品ピッカーでユーザーが選択）

`src/styles/tokens.css` に反映済み。実装はこのトークンを参照すること。

| 部品 | 決定 |
|---|---|
| 紙色 | 薄鼠 `#f2f1ee`（カード背景 `#faf9f7`） |
| 見出し書体 | Zen Old Mincho |
| 本文書体 | BIZ UDPGothic |
| アクセント色 | 朱 `#b3402a` |
| 強調の見せ方 | 下線（アクセント色の太下線） |
| 見出し装飾 | 左バー（アクセント色 5px） |
| 罫線の言語 | 二重罫（3px double） |
| 枠・囲み | カード分離 |
| 角丸 | 小 6px |
| ボタン様式 | 枠＋影（1.5px枠 + 3px3pxベタ影、押下で沈む） |
| ナビ形式 | マストヘッド中央（二重罫下、リンク列は罫下に中央配置） |
| 余白密度 | 詰め（標準の0.75倍） |

### 2026-09-02 デプロイ先の確定

- 公開URL: **https://website.kit-machine-learning.workers.dev**（HTTP 200 で疎通確認済み）
- `.pages.dev` ではなく `.workers.dev`。Cloudflare の Workers 静的アセット方式でホストされているため
- リポジトリ `KIT-MLab/website` の main へ push すると自動デプロイされる想定

### 2026-09-02 ページ構成の決定（案A: 機能別5項目）

| ナビ表記 | パス | 役割 |
|---|---|---|
| ホーム | `/` | 入口 |
| プロジェクトについて | `/about/` | 何をしている団体か・誰が対象か |
| 活動 | `/activities/` | 勉強会・輪読・イベントの記録 |
| 学習 | `/learn/` | 学習コンテンツ。**サンドボックスはこの下に入る** |
| 成果・お知らせ | `/news/` | 作ったもの・発表・募集 |

却下案: 目的別3項目（知る/学ぶ/参加する）、学習主役4項目。

### 2026-09-02 サイト名の表記（案2: 略称＋小さくフル名称）

- マストヘッド大見出し: **KIT 機械学習プロジェクト**（Zen Old Mincho / ホームへのリンク）
- その下に小さくフル名称: AI・機械学習を全課程で0から学べるプロジェクト
- 却下案: フル名称のみ（横に長い）、英字ロゴ KIT MLab（初見に伝わりにくい）

### 2026-09-02 実装済みの骨組み

- `src/layouts/Base.astro` — マストヘッド中央ナビ＋フッター。全ページがこれを使う
- 現在地の表示はアクセント色の太下線（決定済みの「強調の見せ方」を流用）
- 本文は未着手。各ページは見出し＋プレースホルダのみ

### 2026-09-02 サンドボックスの設計決定

ユーザーが選択した3点:

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 画面レイアウト | **窓が段階的に増える**（ステージが進むと表示する窓が加わる） | 最初から固定4分割 / タブ切替 |
| 1ステップの粒度 | **フェーズ＋層の入れ子**（順伝播→損失→逆伝播→更新の4フェーズ。逆伝播の中で層を1つずつ遡れる） | 常に層単位 / 学習1回を1拍 |
| 最初に作る範囲 | **土台＋ステージ1〜3**（実際には1〜5まで実装） | 損失地形を先に / 逆伝播を先に / 設計書だけ |

ユーザーからの追加要望:

- **論理回路のフェーズを入れる**。AND・OR を手で作り、XOR で単層の限界に当たる。これが「なぜ層を重ねるのか」の動機になる。真理値表と入力平面の境界線を連動させる
- **上部にタイムラインバー**をつけ、学習のどの時点まで進んだかを行き来できるようにする。→ エンジンは最初から各ステップの記録を配列で保持する設計にした。バーのUIは学習を実装するステージ6以降で出す

### 2026-09-02 サンドボックスのステージ構成

> **この節は後の作り直しで置き換わった。** ステージ制はやめ、解禁式にした。下の「サンドボックスの目標と解禁」を参照。

| # | 内容 | 状態 |
|---|---|---|
| 1 | ニューロン1個。重み・バイアスを手で動かす | 実装済み |
| 2 | 活性化関数（identity / step / sigmoid / tanh / ReLU） | 実装済み |
| 3 | AND をつくる（真理値表＋入力平面） | 実装済み |
| 4 | OR をつくる | 実装済み |
| 5 | XOR に挑む → 単層では作れないことを確かめる | 実装済み |
| 6 | 層を足して XOR を作る | 未着手 |
| 7 | 損失。手で最小を探す | 未着手 |
| 8 | 勾配降下と学習率（発散・停滞） | 未着手 |
| 9 | SGD / Momentum / Adam を同じ地形で比較 | 未着手 |
| 10 | 逆伝播を層ごとに1ステップずつ | 未着手 |
| 11 | 勾配爆発・勾配消失 | 未着手 |
| 12 | 自由モード | 未着手 |

### 2026-09-02 サンドボックスの技術構成

- **TensorFlow.js は使わない。** 中間層の勾配をすべて取り出して見せるのが目的なので、数値計算は自前実装する。扱うネットは数十パラメータ規模で自前で十分速い
- 計算部 `src/sandbox/engine/` は UI を一切持たない。`forward()` は出力だけでなく各層の z と a をすべて含む記録を返し、表示側はその記録を描くだけ。これで「内部が見える」を構造として保証する
- 表示部 `src/sandbox/views/`、ステージ定義 `src/sandbox/stages.ts`、島本体 `src/sandbox/Sandbox.tsx`
- Astro + React island（`@astrojs/react` を導入、`client:load`）。ページは `/learn/sandbox`
- 図はすべて SVG（ホバーやクリックで要素を選べるようにするため）

### 2026-09-02 サンドボックスの暫定デザイン（ユーザー確認待ち）

> **窓の並びに関する記述は無効。** 作り直しで「主役1枚＋小窓」に変わった。色と書体の項目は引き続き未確認。

決定済みトークンだけで組んだが、以下は新しい要素なので確認が要る:

- **重みの符号の色**: 正＝朱（アクセント）、負＝墨。線の太さが重みの大きさ。2色目のアクセントを入れるかは未決定
- **ノードの塗り**: 値の大きさを濃度で表す（正は朱、負は墨）
- **等幅書体**: トークンでは IBM Plex Mono だが、まだ読み込んでいない。OS の monospace で代替中
- **窓の並び**: 700px 以上で2列固定。ステージ2だけ右下が空く

### 2026-09-02 サンドボックスの作り直し（ユーザーによる差し戻し）

差し戻しの理由:

- スクロールしないと下の情報に届かないのはユーザビリティとして良くない。1画面で済むようにする
- サンドボックスにサイトのナビは要らない。別ウィンドウで開けてもよい
- **説明が多い**。詳しい説明は本に任せる。ここは実際に手を動かせることが目的で、学習そのものより補助・楽しみに近い
- ステージ1〜5の順送りではなく、**要素が解禁されてできることが増える**形にする

決定:

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 解禁の仕組み | **部品箱が増える方式**。目標を常に1つだけ提示し、達成すると部品・道具・窓が増える | 技術ツリー（見取り図） / 最初から全部使える＋実績 |
| ツールバー | **マウスの役割を切り替えるバー**（観察 / 調整 / 設置）。窓の表示オンオフも同じバーに置く（ユーザー提案） | — |
| レイアウト | **主役1枚＋左に小窓**。小窓をクリックすると主役と入れ替わる | 自動分割グリッド / ネットワーク中心固定 |
| 説明の出し方 | **目標1行＋「?」ボタン**。詳細はポップアップでのみ出す | 初回だけ自動表示 / 完全に無言 |
| サイトのナビ | サンドボックスは**全画面専用レイアウト**（`src/layouts/Full.astro`、ナビもフッターもなし）。学習ページからは別タブで開く | — |
| 入力 | 常に (0,0)(0,1)(1,0)(1,1) の**4通り固定**。自由入力モードは廃止 | 自由入力との併存（何を見ているかが二重になるため却下） |

### 2026-09-02 サンドボックスの目標と解禁（実装済み）

| # | 目標 | 達成で解禁されるもの |
|---|---|---|
| 1 | 入力が (1,1) のとき出力を 1.00 以上にする | 部品「活性化関数」／道具「調整」／窓「活性化関数」 |
| 2 | 活性化関数をステップに切り替える | 窓「真理値表」「入力平面」 |
| 3 | 両方が1のときだけ1を出す（AND） | — |
| 4 | どちらかが1なら1を出す（OR） | — |
| 5 | 一方だけが1のとき1を出す（XOR） | **達成不能**。「?」から「2層目を解禁する」を押すと 部品「2層目」／道具「設置」 |
| 6 | 2層目を使って XOR をつくる | （第1章の終わり） |

- 最初から使えるもの: 部品「重み」「バイアス」／道具「観察」／窓「ネットワーク」「計算の中身」
- 道具の中身: 観察＝カーソルを合わせると値が出る、調整＝線やノードを上下ドラッグで重み・バイアスを変える、設置＝中間ノードを足す/外す（最大4）
- 目標5の「達成不能を確かめさせてから層を解禁する」流れが、深層が必要な理由の動機付けになっている

### 2026-09-02 作り直しで分かっている粗（今後の課題）

- **スマホ幅（620px未満）では1画面に収まらない**。本体の中だけ縦スクロールを許す妥協をしている。専用のレイアウトが要る
- 中間ノードを増やすと右パネルのつまみが縦に伸びる。4つで13本
- 「計算の中身」は小窓では文字が小さすぎて読めない（主役に持ってくる前提）

### 2026-09-02 サンドボックスの方向転換（ユーザー発、第2次設計）

> 「サンドボックスの目標と解禁」の6目標は**この決定で置き換わる**。序盤ステージとして形を変えて残す。

ユーザーが触ってみて出した方向: 概念を解禁していく形ではなく、**簡単な回帰・分類を解くという目標のもとで自由にいじる**。先に完成品（学習まで動く本体）を作り、操作のチュートリアルは後から少しずつ足す。詳しい理論は本に任せるのは変わらない。

言語化した骨格:

- **型は「工場建設ゲーム」**。お題（この関数に当てはめろ／この点を分けろ）に対し、モデルを自分で組み立てて、動かして、眺めて、直す。答えは一つではなく、組み方が遊び
- 進行の軸は2本。**ステージ＝お題の難しさ**（平均 → x² → sin → x·y → 分類 …）、**チュートリアル＝操作の種類**（層を置く → 活性化を割り当てる → 学習 → 学習率 → オプティマイザ …）。概念は解禁しない
- 手で重みを置けるのは序盤だけ。x² 以降は学習させないと解けないので、**損失・勾配降下・オプティマイザは後半の飾りではなく本体**
- 活性化を置かなければそのまま動くが線形にしかならない。「x² に直線しか当たらない」を体験させるのが非線形性の説明の代わり
- 真理値表・入力平面は「2入力のときだけ使える観察窓」に格下げ。廃止はしない

| 項目 | 決定 | 却下した案 |
|---|---|---|
| ステージの解き方 | **ステージで変わる**。序盤は手で重みを置いて解ける。以降は構造とハイパーパラメータを決めて学習させ、結果を見て組み直す。手動調整は最後まで残す | 基本は学習 / 基本は手組み |
| キャンバス | **層ブロックを横に並べる**。入力→[層][層]→出力。各ブロックにノード数と活性化。全結合のみ。分岐（concat 等）は将来ブロックとして足せる余地を残す | ノードと線を自由配置 / まずブロック後でノード単位 |
| 触れる設定（最終目標） | 層ごとの活性化・ノード数／学習率・オプティマイザ・エポック／損失関数・重みの初期化／データ側（ノイズ・点数・範囲）。将来: 層ごとの学習率、L1/L2、concat | — |
| 回帰中の主役の窓 | **当てはまりの図**（横軸 x、点がデータ、線がモデル、学習中に線が動く）。ネットワーク図は脇の小窓 | ネットワーク図のまま / 状況で切替 |
| クリア条件 | **損失のしきい値＋任意の縛り**（「パラメータ数〇以下」「層は1つ」など）。縛りが組み方の工夫を促す | しきい値だけ / スコア表示 |
| チュートリアル | **ステージ列の先頭に組み込む**。一本の列で、序盤ステージ（平均・AND・x²…）が操作のチュートリアルを兼ね、毎ステージ新しい操作が1つ増える。XOR は「2層目を置く」の回として残す | 別モード / ジャンル別の列 |
| 学習の操作 | **再生・停止・1ステップ＋履歴バー**。上のバーは学習履歴で、左右にドラッグするとその時点の重み・当てはまり・損失に全窓が戻る。途中からやり直せる | エポック数を指定して実行 |
| 作る順序 | **エンジン（逆伝播・オプティマイザ・履歴）→ ステージの枠組み → 層ブロックUIと当てはまり窓 → 序盤ステージ**。現在の版は同じ場所で上書きし、git に残す。上書き前に現在の2コミットを push | UIの見た目から |

引き継ぐもの: `forward()` が全記録を返すエンジン設計、ツールバー（道具／窓のオンオフ）、主役1枚＋脇の小窓、目標1行＋「?」、全画面レイアウト。捨てるもの: `objectives.ts` の解禁の中身。

### 2026-09-02 第2次設計の細部と進め方の変更

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 層ブロックの見た目 | **案A: ノード列の見出しがブロック**。「層1 · 4 · tanh」の見出しを押すと右の設定がその層になる。列の間の「＋」で層を挿入 | 案B: ブロック列とノード図を上下に分ける |
| 履歴バー | **案A: 損失の曲線そのものがバー**。曲線の上をドラッグするとその時点に戻る。損失の小窓は不要 | 案B: 素のスライダー＋損失は別窓 |
| ステージ列（草案、実測で調整） | 1 平均(層を置く/重み) → 2 AND(活性化) → 3 XOR(中間層/ノード数) → 4 直線(学習) → 5 x²(学習率) → 6 sin x(オプティマイザ, パラメータ≤40) → 7 \|x\|(損失/初期化, 層1つ) → 8 x·y(データ) → 9 円の内外(ミニバッチ) → 10 渦巻き(全部＝自由モード) | — |

進め方の変更（ユーザー指示）:

- **実装はサブエージェント（Opus 等）に任せ、Claude 本体はオーケストレーターとして設計・言語化・検証を担当する**（コスト削減のため）
- **サンドボックスのデザインはサイトのトークンに縛られない。** 「ファイルの情報は割と無視して、自由にやってみてほしい。とらわれないでほしい」。よってサンドボックス内に限り、本ファイル冒頭の「Claude が単独で決めない」ルールと第1次トークンは適用しない。サイト本体（ホーム等）には引き続き適用する

### 2026-09-02 第2次実装への差し戻し（ユーザーが触った感想）

感想: 方向性は良い。「当てはまり」「計算の中身」が小さい。情報量が多く、まず何をすればいいか分からない。操作方法も分からない。ステージ1で層を増やせるとユーザーは層を増やしまくって迷子になる。

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 情報の出し方 | **解禁したものだけ見せる。** 未解禁の操作・窓・パネルは無効表示ではなく存在ごと隠す | 無効表示で「増えるもの」を見せる（前回の指定。情報過多の原因だった） |
| 目標の見せ方 | **画面上部の主役級に大きく。** 見出し＋いまやるべき1手を常時表示 | 1行テキスト |
| 序盤チュートリアル | **1手ずつ指示し、触るまで他は隠す**（ステージ1〜4）。触るべき要素を光らせ、値が動いたら次の1手へ自動で進む。読ませない。ステージ5以降は目標＋?に戻す | 光らせるが順番は自由 |
| ステージ1 | **2→1 のモデルが置かれた状態から始める。** 重み・バイアスの上げ下げで出力がどう変わるかだけ | 白紙から層を置く |
| 層を置く操作の解禁 | **ステージ5 x² で解禁。** XOR は 2-2-1 が置かれた状態で重みを手で組む | ステージ3 XOR で解禁 |
| 主役の窓 | 主役のときは画面いっぱいに描く（余白・パネルを削る） | — |

## 未決定（今後の選択待ち）

- [ ] ステージ一覧のしきい値と縛りの最終値（実装後に実測して決める）
- [ ] 損失地形（2次元切り出し）とボール転がしの窓、逆伝播の層ごと表示の窓、勾配爆発の見せ方
- [ ] 重みの符号の色（現在は正＝朱／負＝墨。2色目のアクセントを入れるか）
- [ ] 等幅書体（トークンは IBM Plex Mono だが未読み込み。OS の monospace で代替中）
- [ ] ホーム以下、各ページの本文と中の部品（ヒーロー・お知らせ一覧・カード等）
- [ ] フッターの様式（現在は暫定で「二重罫＋一行」）
- [ ] ロゴマーク（図形）の有無。現在は文字のみ
- [ ] ダークモード対応の有無

### 2026-09-02 サンドボックス第2次実装（工場建設ゲーム型）

ユーザーの指示により、この回に限りサイトのデザイントークン（紙色・朱・明朝）から外れてよいことになった。
サンドボックスは「道具箱」として独立した見た目を持つ。以下は Claude が決めた内容の記録（次に触るときの前提）。

| 項目 | 決めたこと |
|---|---|
| 地 | 暗い作業台。背景 #171715 / パネル #201f1d / 段 #2a2926・#34322e / 罫 #403d38 |
| 文字 | #e8e4dc、補助 #9c968b。書体は OS の system-ui（サイト本体の明朝・BIZ UD は使わない）。数値はすべて等幅 |
| 符号の色 | **正 = 橙 #e08a3c / 負 = 青緑 #4aa39a**（朱・墨の2色から変更）。回帰の面も分類の領域もこの2色の発散配色で塗る |
| 強調 | 山吹 #f0c14b。選択中のチップ・再生ボタン・モデルの曲線・履歴の曲線に使う |
| 合否 | クリア = 若草 #8fbf6a、縛り違反 = 赤 #d2564a |
| 画面 | 4段の固定グリッド（ステージ／目標／学習操作／本体）。本体は 小窓132px + 主役1枚 + 右パネル254px。ページスクロールなし、右パネルだけ内部スクロール |
| ネットワーク図 | 案A（列の見出しがブロック）。見出しカードに「層1 · 4 · tanh」、クリックで選択、道具「設置」のとき列間に＋・見出しに×・ノード数の±。辺が16本を超えたら重みラベルを消す。図は等倍〜1.5倍までしか拡大しない（文字の大きさを揃えるため） |
| 履歴バー | 案A（損失の曲線そのものがバー）。対数目盛、しきい値を破線で重ねる。左右ドラッグでその時点に戻る。最大4000点、超えたら1つおきに間引く |

ステージ10本としきい値は `src/sandbox/stages.ts` に集約。しきい値は実測して決めてある
（線形だけでは届かず、狙った操作を使えば届く値）。

粗として残っているもの:
- 履歴を巻き戻すとオプティマイザの持ち越し量（Momentum/Adam のモーメント）はリセットされる
- 層の挿入・削除・ノード数の変更をすると重みは引き直しになる（ノード数だけは重なる部分を引き継ぐ）
- スマホ幅は未対応のまま

### 2026-09-03 サンドボックス第3次実装（解禁式＋1手ずつのチュートリアル）

「第2次実装への差し戻し」の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| 解禁 | 未解禁の操作・窓・パネル・ステージ番号は **DOM ごと出さない**。右パネルは中身が無ければ描かない。窓チップ列は解禁2つ以上のときだけ。列構成は `grid-template-columns` を Sandbox.tsx が組み立てる |
| 目標 | 上部に 25px の見出し＋その下に**いまやるべき1手**（橙・点滅の丸つき）。「?」はその横、損失としきい値は右端に小さく |
| チュートリアル | `stages.ts` の `tutorial: TutStep[]`。`targets`（`e:層:出:入` / `n:層:出` / `h:層` / `act` `knob` `play` `hist` `plus` `nodes`、末尾 `*` は前方一致）を光らせ、**ステージ1〜4はそれ以外を操作不能**にする。`done(ctx)` が真になったら自動で次の1手へ。指さす先が図の中なら主役を自動でネットワーク図に切り替える |
| 道具バー | 廃止。観察・調整・設置の切り替えをやめ、解禁済みなら常に触れる |
| 解禁の順 | 1 重み / 2 活性化 / 3 なし（層選択のみ） / 4 学習 / 5 層を置く・ノード数 / 6 オプティマイザ・学習率 / 7 損失・初期化 / 8 データ / 9 バッチ / 10 全部 |
| 初期構成 | 1: 2→1、2: 2→1、3: **2-2-1 が置かれた状態**、4・5: 1→1 |
| 主役 | 余白と見出しを削って本体領域を全部使う。「計算の中身」は主役のとき 16px。ネットワーク図は列が少ないうちは 2.2 倍まで拡大 |
| 自由モード | 上部右端の小さな文字。全解禁＋チュートリアル無効 |

実測（既定の SGD lr 0.05）: 1・2・3 は手置きで損失 0.0000、4 は約 270 歩で 0.0112（目標 0.014）、5 は 1-4-1 tanh で約 4,800 歩で 9.7e-4（目標 0.002）。

残っている粗:
- ステージ6以降のチュートリアルは無し（目標＋「?」のみ）。新しく増えた操作の指さしは未実装
- 「1歩」を高速に連打すると、歩数を数えるチュートリアル（4-②、5-①）が一瞬で終わる
- スマホ幅は未対応のまま

### 2026-09-03 サンドボックス第3次実装への差し戻し（ユーザーが触った感想）

**PC 前提。スマホ対応は課題から外す**（ユーザー明言）。

感想: 指示文の場所が左上すぎて見づらい。損失と目標が右上に薄く小さい。入力は固定でよいが負より正の値のほうが分かりやすい。「入力 2」「出力 1・なし」が何を表すか分からない。重みの調整方法が初見で分からない。右のつまみバーは最初のうちは要らない。

バグ報告: 右のスライダーには上下限があるのに、線を直接ドラッグすると値をどこまでも大きくできる。

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 重みの操作 | **押している間だけ縦の目盛りが出る。** クリックした瞬間に線の脇に上下バーと現在位置が現れ、そのまま上下ドラッグで値が動く。目盛りの範囲がそのまま上下限になり、上記バグも同時に直る | 数値入力 / 右パネルのスライダーのまま |
| 右のつまみパネル | **撤去。** 序盤は細かい調整をしないので出さない。後のステージで解禁 | 常時表示 |
| 指示文と損失の位置 | 上部中央に大きく。左上の小さい行はやめる | — |
| 層ブロックの表記 | 「入力 2」「出力 1・なし」では伝わらない。`入力 x₁ x₂` / `出力 1個・活性化なし` のように自明な表記へ | — |
| ステージ1のデータ | **正の値**にする | 負を含む |

#### ステージ1を「重み調整係」にする（ユーザー発案）

指示して 0.5 に置かせるのではなく、**1点ずつ合わせさせる**。ユーザーが手動で確率的勾配降下をやることになり、ステージ4「▶ を押す」への伏線になる（さっき自分がやったことを機械が高速でやる）。

- 入力ノードの**左**に角丸の箱で今の入力値、出力ノードの**右**に角丸の箱で目標値
- 合っていれば**緑に光って少し膨らみ**、次の点へ。外れていれば**赤くして左右に揺らす**（通らない入力もあることを見せる）。揺らした後は止まって待ち、ドラッグ中も判定し続けて、合った瞬間に緑→次へ
- **連続で通すほど切り替えが速くなる**。終盤は一気に走り抜ける
- クリア条件は問題数ではなく**データを一周**。失敗したら連続数はリセット
- 表示する損失は**今の入力と重みでの損失だけ**。合わせた瞬間 0 になり、次の点で跳ね上がる。この往復が学習の正体
- 適用は**ステージ1だけ**。AND・XOR は真理値表を同時に見る今の形のまま

### 2026-09-03 サンドボックス第4次実装（ステージ1「重み調整係」・縦目盛り）

直前の節の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| 重みの操作 | 線・ノードを **pointerdown した瞬間に、その場に縦の目盛りが出る**（`NetworkDiagram` 内、`createPortal` で body 直下に固定配置）。範囲 −3〜+3・刻み 0.05 が**そのまま上下限**で、`clampW`/`snapW` で丸める。0 の位置に太い印、0.25 ごとに目盛り、整数は長い目盛り。つまみの脇に現在値の札。離すと消える |
| バグ修正 | 図のドラッグが `onWeight(delta)` の加算だったので上限が無かった。**絶対値を渡す `onWeight(li,o,i,value)` に変更**して同じ上下限に揃えた |
| 右のつまみパネル | 解禁項目 `knob` を新設し、**ステージ6で解禁**。ステージ1〜5では右パネルにつまみが出ない（ステージ1は右パネルごと出ない） |
| 上部 | 3列グリッド（余白 / 中央 / 余白）にして、**目標・いまやる1手・損失を上部中央**に。損失の数字は 30px（背の低い画面で 28px）。目標値・正答率は数字の右に小さく |
| 層ブロックの表記 | `入力 x₁ x₂` / `層1 3個・tanh` / `出力 1個・活性化なし`。`ACT_SHORT.identity` を「活性化なし」に |
| ステージ1のデータ | 固定 12 点（x₁,x₂ ∈ [0.2,1.8] の小数第1位、正の値のみ）。目標は (x₁+x₂)/2 |
| ステージ1の進行 | `stages.ts` の `ticker`（tol 0.05 / base 900ms / decay 0.8 / min 180ms / shake 400ms / hintAfter 8）。入力の左と目標の右に角丸の箱を出し、**毎レンダリング判定**（ドラッグ中も走る）。合格 = 緑に光って 1.15→1.0、次の点で外れていたら赤くして 400ms で3往復揺らし、その点で待つ。**連続で通した数がデータ数に届いたらクリア**（＝一周）。失敗で連続数はリセット |
| ステージ1のチュートリアル | 「x₁ から出ている線を押したまま、上下に動かしてみてください」の1手だけ。済んだら出題を始め、以後は指示を出さない。8回落とすと1行ヒント |
| スマホ | 対応しない（PC 前提）。残っている `@media (max-height: 660px)` は 1024×600 のための高さ調整 |

実測: 正解（0.50 / 0.50 / 0.00）を置くと 12 点を 900 → 720 → 576 → … → 180ms と詰めながら約 4.5 秒で走り抜けてクリア。1280×720・1024×600 とも `scrollHeight == innerHeight`。

残っている粗:
- ヒントは「同じ点で8回」ではなく**通算8回**落としたら出している（同じ点で複数回落とすことが構造上できないため）
- 合格の演出中（最大 900ms）に重みを動かしても、その回の合格は取り消されない
- ステージ2・3のチュートリアルの `targets` に、いま存在しない `knob` が残っている（無害）

### 2026-09-03 第4次実装への差し戻し（ユーザーが触った感想）

感想: 光っている線をクリックすれば動かせることは類推できるが、バイアスには案内が無いので初見では動かせない。ドラッグ中に一瞬でも合うと次の問題に進んでしまい、何が正解だったのか分からない。重みの選択幅（±3）が広すぎる。「入る数」「出したい数」は大学生が機械学習を学ぶ表現としては微妙。クリア後の「次のステージ」が小さすぎて見つけにくい。

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 要素の出し方 | **要素一つ＝ステージ一つ。** ステージ1ではバイアスを 0 に固定してロックし、重み2本だけを触らせる | 最初から全部触れる |
| バイアスの解禁 | **バイアス専用のステージを新設**し、ステージ2に挟む（全11本になる）。同じ出題形式で目標を「平均より 0.5 大きい数」にする。重みだけではどう置いても一周できないので、バイアスの役割が手で分かる | AND と同時に解禁 / ステージ1の途中で解禁 |
| 判定のタイミング | **ドラッグ中は判定しない。指を離してから確定して判定する。** 損失の数字だけはドラッグ中も追従する | 常時判定（一瞬かすめただけで通ってしまう） |
| 判定までの間 | 離してから、または新しい問題が出てから **700ms**。連続で通すたびに 50ms ずつ縮め（650, 600, …, 300。8回でおよそ4秒）、以降は **250ms 固定**。失敗したら 700ms に戻す | 一定間隔 |
| 重みの範囲 | ステージ1・2は **±1.5**（±3 は広すぎる）。ステージ3以降は ±3 のまま（AND のしきい値 −1.5 を端に置かないため） | 全ステージ ±3 |
| 箱の表記 | ノードの脇の箱は **「入力」「出力」** | 「入る数」「出したい数」 |
| 層ブロックの表記 | 層を表しているので **「入力層」「出力層」**、中間層ができたら **「中間層」** | 「入力」「出力」 |
| 次のステージ | **右下に、層ブロックの箱と同じくらいの大きさで置く。** クリアするまでは灰色、クリアしたら押せそうな色にする。文言は「次のステージ」 | 上部に小さく |

参考（ユーザー談、今後の設計に効く）: 人が何かを操作して「できた」と確認するには最低 700ms 程度が要る。1000ms だと遅く感じる人もいる。

### 2026-09-03 サンドボックス第5次実装（バイアスのステージ・離してから判定）

直前の節の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| ステージ1のバイアス | 解禁項目 `bias` を新設。ステージ1では未解禁なので出力の丸を押しても目盛りが出ない。**ロックの表示は出さない**（鍵印・薄い丸・「バイアス 0 で固定」は一度入れたが、まだ解禁していないものの存在を知らせないため撤去。ホバーの説明文と「?」の本文からもバイアスの語を外した）。初期バイアスは `initNetwork` が常に 0 を入れるので 0 のまま |
| ステージ2（新設） | `id: 'offset'`／目標「平均より 0.5 大きい数を出す」。点はステージ1と同じ12点、目標だけ +0.5。`unlocks: ['bias']` でここからバイアスが触れる。チュートリアルは「出力の丸を押したまま、上下に動かしてみてください」の1手だけ。以降のステージ番号は1つずつ繰り下げ（AND 3 / XOR 4 / 直線 5 / 放物線 6 / 波 7 / 折れ線 8 / かけ算 9 / 円 10 / 渦巻き 11）。`LOCK_UNTIL` も 3→4 |
| 判定のタイミング | `Tick.phase` を `idle / armed / pass / fail` に。**予約するのは「新しい問題が出た」「指を離した」の2つだけ**。`NetworkDiagram` に `onDrag(active)` を足し、pointerdown で予約を取り消して `idle` に戻し、pointerup で予約し直す。ドラッグ中は判定が走らない（損失の数字だけは毎レンダリング更新されるので追従する） |
| 判定までの間 | `judgeDelay(t, streak) = max(250, 700 − 50 × streak)`。合格 → 緑のポップ 280ms → 次の問題 → また予約。不合格 → 赤く 400ms 揺らして止まり、**予約しない**（連続数は 0、次は 700ms に戻る）。揺れの最中に直して離した場合だけ、揺れを打ち切って予約に入る |
| 重みの範囲 | `Stage.wRange`（省略時 3）。ステージ1・2は 1.5。`NetworkDiagram` の目盛り・クランプ・刻み・右パネルのつまみが全部これに従う |
| 表記 | ノード脇の箱は「入力」「出力」。層ブロックの見出しは「入力層」「出力層」、中間層は1つなら「中間層」、複数なら「中間層1」「中間層2」。右パネルの層の箱も同じ呼び方に揃えた |
| 次のステージ | `.sb-next` を画面右下の固定ボタン（150×46px）に作り直し、常時表示。クリア前は灰色＋`disabled`、クリアで若草に変わる。右パネルは下に 68px 余白を足して被らないようにした |

実測（正解 0.50 / 0.50、ステージ2はバイアス 0.50 を置いた場合）: 判定の間隔は
700 → 650 → 600 → 550 → 500 → 450 → 400 → 350 → 300 → 250 → 250 → 250 ms で
ステージ1・2とも12点を一周してクリア。ドラッグ中に正解値を3回通しても合格せず（損失の表示は 0.0000 に追従）、
離してから初めて 700ms の予約が入る。不合格のあとは 400ms 揺れて予約なしで停止し、直して離すと 700ms から再開する。
1280×720・1024×600 とも全ステージで `scrollHeight == innerHeight`、横も同じ。

残っている粗:
- 合格の演出（280ms）と待ち時間は別勘定なので、1点あたりの実時間は「演出 280ms ＋ 間 250〜700ms」になる
- 合格の演出中に重みを動かしても、その回の合格は取り消されない（第4次実装から据え置き）
- ヒントは「同じ点で8回」ではなく通算8回落としたら出している（第4次実装から据え置き）
- 右下の「次のステージ」は後半のステージで右パネルの上に重なる（影を付けて手前だと分かるようにしてある）

### 2026-09-03 設計原則の抽出と、以後の進め方

- これまでの修正指示から核となる考え方を抜き出し、**`design/PRINCIPLES.md`** に置いた。以後の実装ブリーフには必ずこれを添える。個別の決定はこのファイル、判断基準は PRINCIPLES.md
- **モード切替バー（観察／調整／設置）は後半で復活する見込み。** 現在の廃止された状態は叩き台としては良く、復活時に中身は変わる（ユーザー談）
- **次にやること: サンドボックスを磨き上げ続ける。** 後半ステージや未実装の窓（損失地形・逆伝播の層ごと表示・勾配爆発）より、序盤から順に体験を仕上げるほうを優先する。ユーザーは次にステージ2を見る
- **細かい修正指示を数多く出す運用なので、実装のサブエージェントは Sonnet を使う**（Opus は設計・監査など判断の重い場面に限る）

### 2026-09-03 第6次実装への差し戻し（ステージ1の詰めとステージ3、活性化のノード単位化）

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 損失の表示 | **損失という概念を学ぶ段階（学習が解禁されるステージ5）から出す。** ステージ1・2は「通した数 n/12」、3・4は真理値表の○×が目標の表示になる。損失は ▶ で下げる対象として初めて登場する | ステージ1・2だけ隠す |
| 目標の表示 | ステージ1では「通した数 n/12」を**主役級に大きく**。いまは小さく薄くて何がゴールか分かりにくい | — |
| 判定間隔 | **700 → 700 → 600 → 500 → 400 → 300 → 200 → 100 → 以降 100ms。** 2回目までは 700ms のままにし、そこから 100ms ずつ加速する | 50ms ずつ縮めて 250ms 止まり（前回の実装） |
| ステージ間の状態 | **ステージごとに状態を保存する。** 戻ればその時の重み・クリア状態がそのまま見られる。ステージ1をクリアして2へ進み、1に戻ると重みが変わっていてクリアしていない状態に見えるのを直す | ステージ1の設定を2に引き継ぐ（ステージ3以降で破綻するため却下） |
| 計算の中身 | **窓から外す。** ツールバーに入れるほどのものではない | 残す |
| 入力平面（旧「当てはまり」） | 軸が x₁・x₂ であることが分かる形にする。「この点はこの入力に対応するのか」と分かれば価値が出る。名前も見直す | 現状のまま |
| 真理値表 | 残す価値が高い。小窓が小さすぎるので読める大きさにする | — |
| **活性化関数の単位** | **層ごとではなくノードごとに適用する。** エンジンの `Layer.act` をノードごとの配列に変える | 層ごと（現状） |
| 活性化の選び方 | **各ノードの脇に、いまの活性化の小さなグラフ印を常時出し、押すとその場で選択肢が開く。** 印がそのまま「このノードに何が入っているか」の表示を兼ねる | 右クリック（初見で気づかない） |
| 活性化の見せ方 | **選択肢には具体的なグラフを出す。** 「ReLU」と言われて分かる人は想定しない。形がひと目で分かること。ホバーで数式などの詳細（例: x > 0 なら x、x ≤ 0 なら 0） | 名前だけ |
| モード切替バー | 「ツールバーから活性化を選ぶ→カーソルが塗る状態になる→ノードを次々クリック」の方式は、**中間層やノードが増えてから解禁**する形で後に入れる | 今すぐ入れる |

### 2026-09-03 サンドボックス第7次実装（活性化のノード単位化、損失の出し方、判定間隔、ステージ保存、ステージ3の窓）

直前の節「第6次実装への差し戻し」の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| 活性化のノード単位化 | エンジンの `Layer.act: ActivationId` を `Layer.acts: ActivationId[]`（ノードごと、`b` と同じ長さ）に変更。`forward`/`backward`/`applyStep`/`cloneNetwork`/`initNetwork` を対応させた。`Shape.acts` は層ごと1つのまま（新しい層を作るときの初期値）で、作った後に `Layer.acts` を個別に書き換える運用 |
| ノード脇の印 | `NetworkDiagram` の各出力ノードの右肩に小さな折れ線アイコン（`ActivationIcon.tsx`、新規）を常時表示（活性化解禁ステージのみ）。押すとその場に選択肢のポップアップが開き、5つの活性化それぞれを具体的なグラフで示す。ホバーで数式と説明文を表示（既存の `onHover` 経路を再利用）。右パネルの層単位の活性化チップ行は撤去 |
| 損失の表示 | ステージ1・2（ティッカー式）は「通した数 n/12」を主役級（40px）に。ステージ3・4（AND・XOR、`kind:'logic'`）は「正解 n/4」を同じ大きさで。ステージ5以降は従来どおり損失を表示 |
| 判定間隔 | `judgeDelay(streak) = streak <= 1 ? 700 : Math.max(100, 700 - 100 * (streak - 1))` に変更。`Ticker` 型から `base`/`stepDown`/`min` を削除（固定式になったため） |
| ステージごとの状態保存 | `Sandbox.tsx` に `stageStates: Map<number, StageState>` を追加。ステージを離れるときに現在の状態（ネット・履歴・ティッカー進捗・データ設定・学習設定・チュートリアル進捗など）を控え、戻ったときに復元する。いま見ているステージのタブを押しても何もしない（誤って自分自身の状態を巻き戻さないためのガード）。新設の「最初から」ボタン（常時表示）で、いま見ているステージだけを初回訪問時の姿に戻す |
| 計算の中身の撤去 | `ViewId` から `'calc'` を削除。ステージ定義の `views` と `Sandbox.tsx` の描画・自由モードの窓一覧から除いた。`Inspector.tsx` 自体は残し、型だけ `acts` 対応させた（今回は参照なし） |
| 真理値表 | 小窓の文字を 10px→11.5px、余白を詰め直して読める大きさに。AND・XOR など論理回路のステージだけ小窓の列幅を 132px→190px に広げた |
| 入力平面（旧「当てはまり」の使い回し） | 論理回路のステージでは窓名を「入力平面」に変更（`fit` の表示名をステージの種類で出し分け）。0の軸に加えて1の破線目盛りを追加。軸ラベル x₁・x₂ は小窓でも常時表示。真理値表の行を選ぶと対応する点が光る（`selected`/`onSelect` を追加、双方向） |

実測: ステージ1・2の判定間隔は 700→700→600→500→400→300→200→100→100…（280ms の合格演出込みで実測しても式どおり）。ステージ1・2・3をクリアしたあと別ステージへ移動し、上部のタブで1へ戻ると重み・クリア済み・「次のステージ」の状態がそのまま残ることを確認。自由モードでステージ6の中間層4ノードに ReLU/tanh/シグモイド/活性化なしを1つずつ割り当てて学習させ、損失が 0.24 → 0.003 まで下がることを確認。`.scratch/check.ts` に数値微分照合と学習収束の両方でノード混在ケースを追加し ALL PASS。1280×720・1024×600 とも `scrollHeight == innerHeight`（横も）。

残っている粗:
- 「計算の中身」（Inspector.tsx）は型だけ直して置いてあるだけで、どこからも参照されない
- 活性化のポップアップの位置は毎回クリック位置基準で出すため、画面端に近いノードでは配置がやや窮屈になることがある
- モード切替バー（ツールバーから活性化を選んで塗る）は今回も見送り

### 2026-09-03 第7次実装への差し戻し（ステージ3の作り直しと活性化の選び方）

ユーザーの感想: 上部の目標まわりが余白を余らせている。右上の「窓／ネットワーク／真理表／入力平面」の表記は不要。「ネットワーク｜データ 1/4 (0.00,0.00→0.00)」は誰も見ない。**そしてあの大きさの文字はユーザーには見えていないのと同じ**。真理表も入力平面も小さすぎる。ノードの脇で選択肢を開く方式はやはり微妙。

| 項目 | 決定 | 却下した案 |
|---|---|---|
| 上部の目標 | 余白を使い切るまで**拡張する**。せっかくの余白を空けたまま小さく置かない | 現状 |
| 窓のオンオフ表記 | 右上の「窓／ネットワーク／…」は**消す** | 場所を変える |
| データ送りの表記 | 「ネットワーク｜データ 1/4 (…)」は**消す**。誰も見ない | 残す |
| 真理値表 | ○×をやめ、**合っている行を緑で覆い、間違っている行は赤**。ぱっと見て済むようにする。**大きくする**（単体表示でも小さすぎる） | ○×の記号 |
| 入力平面 | **どんな線が引ければよいのかを薄く背景に忍ばせる**。大きさは単体表示のときくらいを常時確保する | 現状 |
| 真理値表・入力平面の位置づけ | 序盤の数ステージで強制的に出すだけの道具。上級者向けの汎用ツールにはしない（AND/OR を選んでノードを指定して…という使い方は不要） | 汎用ツール化 |
| ステージ3の活性化の選択肢 | **「なし」と「ステップ」の2つだけ**。初登場の段でそれ以外は要らない | 5つ全部 |
| 活性化の選び方 | **左に縦のツールバーを作り、「活性化関数」の項目を置く。押すと全画面が選択画面になる**（縦2×横3程度の大きな枠で覆う）。グラフは抽象画にせず、数字と軸を正確に、大きく、余白を詰めて描く。**選ぶとカーソルが「塗る」状態になり、ノードをクリックして適用**（連続して何個でも塗れる） | ノードの脇の印から開く（実装したが却下） |
| デバッグ用の道具 | **ホバーで箱が光り、名前と寸法が出る。さらに文字サイズ・余白・幅をその場でスライダーで動かし、「この値にして」と貼り付けられるテキストを出せる。** 寸法の話を言葉ではなく値で受け渡すため | 見るだけ |
| コミット | 細かく分けすぎず、適度な粒度でまとめてよい（ユーザー了承） | — |

### 2026-09-03 サンドボックス第8次実装（ステージ3の作り直しと活性化ツールバー、デバッグ道具）

直前の節「第7次実装への差し戻し」の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| 論理回路の専用レイアウト | `stage.kind === 'logic'`（ステージ3 AND・4 XOR）だけ、汎用の小窓＋右パネンルを使わず専用の `.sb-logic` グリッドを描く。**真理値表を上段に横いっぱい、下段を左＝ネットワーク／右＝入力平面**に分けた。入力平面は正方形なので、本体の高さをまるごと使える専用の列（`grid-row: 1 / -1`）にすることで、真理値表と高さを取り合わない形にした |
| 窓のオンオフ表記・データ送りの行 | `Sandbox.tsx` の `off` state と「窓」チップ列、`sampleNav`（「ネットワーク｜データ 1/4 …」）を丸ごと削除。関連 CSS（`.sb-cap`、`.sb-samp`）も削除 |
| 真理値表 | ○×をやめ、`data-ok` 属性で行全体を緑／赤の地色で覆う（`sb-truth tbody tr[data-ok='true'/'false'] td`）。選択行はオレンジの背景の代わりに黄の内側リングで示す。文字サイズを 24px（1024×600 では 19px）に拡大 |
| 入力平面の背景ヒント | `Stage.logicHint?: [w1,w2,b]` を追加し、AND だけ `[1,1,-1.5]` を設定。`DecisionPlane` に `hint` prop を追加し、現在の重みとは独立に薄い破線でその境界線を描く（`.sb-plane__hint`、opacity 0.22） |
| 上部の目標 | `.sb-goal__h` を 25px→36px、`.sb-meter__v--big`（通した数／正解の表示）を 40px→46px に拡大。ただし真理値表・入力平面の高さを圧迫しすぎないよう、行間の余白は逆に詰めた（padding 7/9px→9/11px、`.sb-goal__now` 16px→17px） |
| 活性化の選び方 | 左端に幅68pxの縦ツールバー `.sb-toolbar`（`can('activation')` のときだけ表示）を新設。押すと `ActivationPicker.tsx`（新規）が全画面を覆い、`stage.activationChoices ?? 全部` を大きな枠（2択なら横2、5択なら3×2）で見せる。各枠は軸目盛り（−4〜4、活性化ごとの `ticksY`）・名前・正確な式を実寸で描画。選ぶと画面が戻り `paintId` が立ち、`NetworkDiagram` のノードをクリックすると `onPaint` 経由でその場に適用される（連続して何個でも）。Esc かツールバー再クリックで解除。ノード脇の印（`ActivationIcon`）は表示専用に変更し、旧ポップアップ（`ActMenu`／`.sb-actmenu*`）は削除 |
| 活性化の選択肢 | `Stage.activationChoices` を新設。ステージ3・4 は `['identity','step']` の2つだけ、それ以外は省略時に全5種（`activationChoicesFor()`） |
| 活性化の式表記 | `activations.ts` の `step`／`relu` の `formula` を「z ≥ 0 なら 1、z < 0 なら 0」のような場合分け表記に変更。グラフの縦軸目盛り `ticksY` を活性化ごとに追加 |
| デバッグ道具 | `DebugInspector.tsx`（新規）。`Ctrl+Shift+D` でのみ有効化（通常表示では一切出ない）。有効時はホバーした要素に枠と名前（`.sb-xxx` クラス名）・寸法・文字サイズを表示し、クリックで選択すると右下にパネルが出て文字サイズ・余白・幅をスライダーでその場変更できる。変更差分（例:「.sb-truth の文字サイズ 11.5px → 18px」）をテキストとしてクリップボードにコピーするボタン付き |

実測（1280×720、ステージ3 AND、チュートリアル進行中の実際の画面）: 目標見出し 36px、真理値表 410×192px（文字 24px）、入力平面 516×501px（単体表示時の目安 ≈487px と同等以上）。1024×600 では目標見出し 27px、真理値表 271×169px（文字 19px）、入力平面 407×406px。いずれも `scrollHeight === innerHeight` かつ横も一致。合成した重み `(1,1,-1.5)` を実際にドラッグで置いて4/4正解・クリアを確認。

残っている粗:
- `ActivationChart.tsx`（stage7以降の「活性化関数」窓）は `var(--color-ink)` 等サイト本体のトークン名を参照しており、サンドボックスの `--sb-*` と噛み合っていない。今回の変更前から存在する不整合で、今回は触っていない
- 活性化選択画面のグラフは `identity` のように表示レンジ外に伸びる関数だと線が枠上端で見切れる（意図的な範囲外クリップで、致命的ではない）
- デバッグ道具のクリップボードコピーは `navigator.clipboard.writeText` に依存する。自動化ブラウザなど clipboard 権限が無い環境では失敗するが、通常のユーザーのブラウザでは問題なく動く想定

### 2026-09-03 第9次実装への差し戻し（ステージ3の配置と、最初の1手の締め付け）

| 項目 | 決定 | 備考 |
|---|---|---|
| ステージ3・4の配置 | **目標帯（`.sb-goal`）を左に切り詰め、空いた右上に真理値表を移す**（入力平面の上）。浮いた分をネットワーク図に回して大きくする。真理値表と入力平面が大きくなったのは良いが、ネットワークが小さくなりすぎた | — |
| ステージ3の開始時 | **活性化関数のツールバー以外を半透明で覆い、操作できなくする。** いまは出力ノードと活性化関数が同時に光っていて、何をすべきか分からない。最初にすべきことは活性化を選ぶことなので、そこだけを残す | **何が操作できるかの制限も大事な設計**（ユーザー談） |
| 活性化の選択画面 | **いま選ばれているものを、押し込まれたボタンとして常に表す。** 少し小さくして縁を暗くし、押し込んでいる感を出す。選択が移れば押し込みも移る（ステップを選べばステップが押し込まれる）。ラジオボタンをボタンの見た目でやるもので、**締め付けの仕掛けとは別物**。ステージを問わず常にそう振る舞う | 押し込みを締め付けの手段と読み違えたのを訂正 |
| ステージ3の進行 | **ステップを選ばなければ進めない。** 選んだら次はノードをクリックするよう誘導し、活性化の適用の仕方を覚えてもらう | 原則3・4の合流 |
| 真理値表の列見出し | `z` だけでは何の値か伝わらず、**重み w と読み違えられた**。z は加重和（活性化を通す前）、w は辺の重み。**「加重和 z」のように何の値かが分かる見出しにする** | ユーザーの指摘は前提が違ったが、表記の落ち度が原因 |
| バグ | 活性化の選択画面で、軸ラベル `a` `z` と目盛りの数字が重なっている | — |

### 2026-09-04 サンドボックス第10次実装（ステージ3・4の配置換えと最初の1手の締め付け）

直前の節「第9次実装への差し戻し」の決定を実装した記録。

| 項目 | 実装 |
|---|---|
| ステージ3・4の配置 | `.sb-goal` を論理回路ステージだけ `.sb-goal--logic` に差し替え、中央揃えをやめて左寄せ・幅は内容ぶんに。`.sb-logic` を `grid-template-areas: "goal truth" / "net plane"` の2×2に組み替え、目標（左上）・真理値表（右上）・ネットワーク（左下）・入力平面（右下）にした。目標の行は内容ぶんの高さしか取らないので、そのぶんがまるごとネットワーク図に回る |
| ステージ3の締め付け | `Sandbox.tsx` に `actLock`（`stage.id==='and' && tutOn && tut.i<=1`）を追加し、`veilPanel(panel)` で `.sb-toolbar` / `.sb-logic__truth` / `.sb-logic__net` / `.sb-logic__plane` の各パネルに半透明の `<div className="sb-veil">` を重ねる（`position:relative` の親の中に `position:absolute; inset:0; pointer-events:auto` で敷く）。`tut.i===0`（活性化を選ぶ）はツールバーだけ見せ、`tut.i===1`（ノードに塗る）はネットワーク図だけ見せる。チュートリアル中だけ・自由モードでは `tutOn` が false になり自動的に外れる |
| ステージ3のチュートリアル分割 | `stages.ts` の AND の最初の1手を2段に分割（`targets:['tool:act']` で選択画面を開いてステップを選ぶ／`targets:['n:0:*']` でノードに塗る）。`TutCtx` に `paintId` を足し、1段目の `done` は `paintId==='step'` で判定する |
| 「なし」を選べなくする締め付け | `ActivationPicker` の `onPick` を呼ぶ前に、`Sandbox.tsx` 側で `actLock && tut.i===0 && id==='identity'` なら無視する（選択画面は開いたまま、状態も変わらない）。締め付けが外れた自由モードや他ステージでは通常どおり選べる |
| 活性化選択画面の「押し込み」表示 | いちばん最近選んだ活性化を覚える `lastAct` 状態（既定値 `'identity'`）を新設し、`ActivationPicker` に `current` として渡す。選ぶたびに `data-current` が移り、CSS で少し縮小・縁を暗く・内側に影（`box-shadow: inset`）を入れて押し込んで見せる。締め付け（なしを選べない）とは完全に独立した仕組みで、ステージを問わず選択画面を開くたびに効く |
| 軸ラベルの重なり | `ActivationPicker.tsx` の `Graph` で `PAD_T` を14→30、`PAD_R` を14→34に広げ、`a` ラベルを目盛りの上（`PAD_T-16`）、`z` ラベルを目盛りの外側（`GW-6`）に離した |
| 真理値表の列見出し | `z` を2行見出し「加重和／z」に変更（`TruthTable.tsx` の `<th>` を2つの `<span>` に、`sandbox.css` に `.sb-truth__zh` / `.sb-truth__zh--main`） |
| 死んだ CSS の掃除 | `.sb-box h2` を実際の要素に合わせて `.sb-box h3` に修正（放置すると見出しが無地の既定スタイルになるので実質バグ修正）。該当要素の無い `.sb-logic__panel h2` と、到達不能な `.sb-axis--small` `.sb-truth--small` を削除 |

実測（1280×720、ステージ3 AND）: ネットワーク図は配置換え前 335×314px → 後 466×436px（面積で約1.8倍）。真理値表 431×200px→416×221px、入力平面 516×520px→438×424px と少し譲ったぶんをネットワークに回した。1024×600 でも同じ傾向（ネットワーク図 264×204px→445×344px）。締め付けの各段階は実測で確認: 開始時は `.sb-toolbar` だけ veil なし・他3枚は veil あり、ノードクリック後は `.sb-logic__net` だけ veil なし、塗り終えると4枚とも veil なしに戻る。「なし」クリックはピッカーを閉じない（`actLock` 中のみ）。ステージ1→2→3を実際の「次のステージ」操作で通しクリアし、活性化選択→ノード塗り→重み調整（1,1,-1.5）→4/4 まで確認。1280×720・1024×600 とも全ステージで `scrollHeight === innerHeight`（横も一致）。

残っている粗:
- `.sb-goal--logic` は自身の DOM 幅はネットワーク図の列いっぱいまであるが、背景・枠を持たないため見た目には左詰めのテキストにしか見えない（意図どおりだが、将来ここに枠を付けたくなったときは幅を詰め直す必要がある）
- 真理値表の2行見出し「加重和／z」は AND・XOR の専用レイアウトでしか使わない（`TruthTable` の他の呼び出し口はすべてこの見出しを共有するが、小窓表示自体が既に到達不能なので実害はない）
- ステージ4（XOR）は締め付けの対象外のまま（活性化の選択を伴わないため、今回の締め付けは AND だけに絞ってある）

---

# 2026-09-11 方向転換: ゲームから学習教材へ

> **この行より上のサンドボックス関連の決定は、すべてこの方向転換で置き換わった。**
> ステージ制・解禁・出題ティッカー・締め付け等のゲーム設計は、教材内の補助的な実験としてしか残らない。
> 新しい仕様は `design/spec/` に置く。本ファイルは以後、方針の変更点だけを日付順に記録する。
> `design/PRINCIPLES.md` の8原則のうち、画面設計に関するもの（原則4〜7）は引き続き有効。
> 原則1〜3（情報を出さない・要素を絞る・答えを言わない）は**教材では適用しない**。教材は説明する場である。

ユーザーの判断: ゲーム感覚を目指していたが、しっかりとした学習教材にする。

| 項目 | 決定 |
|---|---|
| 到達目標 | 情報系でない理系の大学1年・機械学習もコンピュータも初心者 → 大学3年の情報学生程度 |
| 分量 | 一年分、80〜120時間 |
| 範囲 | 数学と Python の基礎から。第0部として Python 編を置く |
| Python の実行 | 前半はサイト内（Pyodide）。後半は uv で各自の環境を作り PyTorch を動かす。ブラウザで torch は動かない |
| Python と機械学習の並べ方 | まず Python 編で基礎中の基礎。細かい部分は必要になったところで差し込むハイブリッド |
| レベル別 | 3段階（超絶初心者 / PC操作は一般レベル / 何か言語を少し触った）。**入り口と補助の量だけが変わる。本文は共通** |
| 深さの作り方 | **同じ話題を2周する。** 第1周は浅く全体、第2周で同じ話題を厳密に。最初は「逆から数字を伝えるらしい」でよいが、後で必ず詳しく戻る。過度に幼稚にはしない |
| 演習 | コードを書いて自動採点／選択式・数値の確認問題／実験を操作して達成。**記述の自己確認は採用しない**（適当に飛ばせてしまうため） |
| 進度 | Cloudflare のデータベースに保存し、先生が見られるようにする |
| 先生の画面 | 誰がどこまで進んだか／どこで詰まっているか／問いごとの正答率／提出コードを読む／**コメントを返す**。**先生の権限は後から付与できる**（メンバーが増える前提） |
| 識別 | 外部にも公開する。内部と外部を見分けられること。簡単ななりすましは防ぐ。クラスコードか ID 発行 |
| 既存の Colab 教材（8回分） | **サイトに吸収して作り直す。** 項目の並びと第6回の「壊れたコードを直す」は資産として使う |
| 使われ方 | 週一の勉強会で使い、終わらなかった分と宿題を自宅で |
| 第1期の範囲 | 土台一式（レベル選択・ログイン・進度保存・サイト内 Python 実行・自動採点・先生画面）＋ Python 編の最初の数章。**最初のメンバーの定着に関わるので作り込む** |
| 進め方 | Claude が仕様を詰め、Sonnet のサブエージェントが実装する。**仕様書に解釈の余地をほとんど残さない** |
| 文章 | AI 特有のキャッチーな言い回しを避ける。過度な抽象表現を避ける（「条件を分岐する方法」ではなく「if文」） |
| 数学の前提（2026-09-23） | **数IIはできる前提。** `x**a` の微分くらいは当然知っているものとして進める。数IIIの内容は前提にしない |
| 章の組み立て方（2026-09-23） | **その章を終えたときに何が分かっているかを先に決め、そこから節を組む** |
| 学習者の像（2026-09-23） | 利用者の言葉:「難しいのは何を与えたかを知っていることではなく、まだ何が分かっていない可能性があるかを推測すること」。**部や章を組む前に、その時点の学習者が「確実にできること／あやふやかもしれないこと／まだ出会っていないこと」を書き出し、そこから逆算する** |
| 第1部と第2部の分け方（2026-09-23） | 第1部に numpy の第8章を足して8章に、第2部は ベクトルと行列／変化率と傾き／確率の初歩 の3章に。数学の前提と勾配降下の置き場所は `00-overview.md` 第2.2節 |
| 点数表（2026-09-23） | **第2部でも続け、第3部の予測につなぐ** |
| 週1の集まり（2026-09-24） | **集まり＝実戦（本物のデータ・コンペ）、Web の教材＝仕組み。**順番は「集まりで体験 → あとで教材を読む」。事前学習をしてこなくても集まりに参加できる形にする。題材は Kaggle の Titanic から。ノートは Colab で、リポジトリの外（`../集まり資料/`）に置く |
| メンバーの画面（2026-09-24） | 一般向けは教材の公開まで（ついで）。**ログインしたメンバー（招待コードが internal）には別の使い心地の画面**を出す。毎週の資料は**データベースに置き、運営の画面から書く**（リポジトリが公開のため）。出すもの: 今週の集まり・自分の進み具合・質問への返事・次の集まりの日時と場所・チームの記録。管理者の画面はメンバーと同じで、**右下に運営の画面への入口**。運営の画面では取り組んだ時間帯と時間を見たい。**並べ方は案A「今週が主役」に決定。**メンバーの画面に時間は出さない（推定なので曖昧、利用者の判断）。活動は記録して運営が見る。時間の推定方法は一任され、「画面が見えていて直前120秒以内に入力があった分」を数える形にした（`20-platform.md` 第13章） |
| 教材の節のページの配置（2026-09-24） | **案A「帯を固定」**（試作 https://claude.ai/artifact/GixWexiwVkW1D3ShykmT6Y ）。節のページは上に細い帯（サイト名・メニュー・アカウント）を固定し、左に節の一覧・用語集・質問を固定する。スクロールしても動かない。アカウントは右上で、ホームと `/learn/` を含む全ページに出す。狭い画面では左の欄を本文の上のボタンに畳む。第0.1節の「右側の欄」の説明と課題を合わせて直す |
| 画面の言葉（2026-09-24） | 利用者の指摘「不自然な表現は直す」。**「メンバーの画面」→「マイページ」、「入る／出る」→「ログイン／ログアウト」、「運営の画面へ」→「管理画面へ」。**画面に出す言葉は、世の中のウェブサイトで普通に使われている呼び名に合わせる（`10-lesson-and-writing.md` 第4.3節「名前があるものは名前で呼ぶ」と同じ考え）。ログイン中は右上のメニューに利用者IDを出す（利用者が表示名でログインしようとして入れなかったため） |
| 実験（2026-09-23） | 谷へ近づく様子、学習率が大きすぎて発散する様子などは、アニメーションや手で動かせる実験で見せたい（利用者の希望）。`00-overview.md` 第5節の「節に埋め込む実験」として作る |

## 既存の Colab 教材の評価（Claude が全8回を読んだ所見）

「単体では学習として弱い」の中身:

1. **課題がすべて直前に見たものの書き写し。** 何をするか自分で決める段が、第1回の最終課題以外にない
2. **合っているか確かめる手段がない。** 期待される出力は書いてあるが、照合するのは学習者の目だけ
3. **「なぜ」がほとんど無く、文法の一覧になっている。** 変数には動機があるが、リスト・関数・NumPy はほぼ構文の紹介
4. **回をまたいで積み上がるものがない。** 各回が独立していて、前回作ったものが今回育つ線がない
5. **機械学習に向かっている感じがしない。** 第7回で NumPy が出るが、なぜ配列が要るのかが機械学習側と繋がっていない

良い点: **第6回（壊れたコードを直す回）は良い。** `prit`、全角の `（`、`if score = 80:`、`print(animals[1])` など実際に踏む地雷ばかり。ただし1回にまとめず各回に分散させる。

サイトが本やノートブックに勝てる点: 答えを判定できる／エラー文に反応できる／どこまで進んだか覚えている／図が動く／読む・動かす・直す・答えるを画面を移動せずに回せる。
