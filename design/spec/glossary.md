# 用語集

`10-lesson-and-writing.md` 第5章の登録先。

- 新しい用語はこの表に足す。登録がない語を本文で使ってはいけない
- 節の frontmatter の `terms` は、この表にある語だけを書く（`scripts/check-lessons.mjs` 検査12）
- カタカナ語は「英語」の列に綴りを書く。本文の初出でも併記する
- 定義は1〜2文

| 語 | 英語 | 定義 | 初出の章 |
|---|---|---|---|
| 半角 | half-width | 英数字や記号を1文字ぶんの半分の幅で表示する文字の種類。Python のコードは半角で打つ。 | 00-start |
| 全角 | full-width | 半角の2倍の幅で表示される文字の種類。日本語入力がオンのときに打たれる数字や記号は全角になり、Python のコードとしては読み取れない。 | 00-start |
| 拡張子 | file extension | ファイル名の末尾に付く、ファイルの種類を表す文字列。`.py` は Python のプログラムを表す。 | 00-start |
| ファイル | file | 保存した中身のひとまとまりに、名前が付いたもの。 | 00-start |
| フォルダ | folder | ファイルを入れておく場所。フォルダの中に別のフォルダも入れられる。 | 00-start |
| ディレクトリ | directory | フォルダの別の呼び名。 | 00-start |
| エクスプローラー | Explorer | ファイルとフォルダを見て操作するための画面。 | 00-start |
| クリック | click | マウスの左のボタンを1回押す操作。 | 00-start |
| ドラッグ | drag | マウスのボタンを押したまま動かして、離す操作。 | 00-start |
| 右クリック | right-click | マウスの右のボタンを1回押す操作。 | 00-start |
| デスクトップ | desktop | パソコンを開いたときに最初に見える画面。 | 00-start |
| ドキュメント | Documents | 文章などのファイルを保存する、決まった場所。 | 00-start |
| アイコン | icon | フォルダやファイルを表す小さな絵。 | 00-start |
| メニュー | menu | 選べる項目が並んだ一覧。 | 00-start |
| print | | 値を画面に表示する関数。かっこの中に表示したいものを書く。 | 01-python |
| 文字列 | string | 文字を並べたもの。前後を `"` で囲んで書く。 | 01-python |
| 引数 | | 関数のかっこの中に渡す値。 | 01-python |
| 関数 | | 名前を付けた処理のまとまり。名前とかっこで呼び出す。 | 01-python |
| input | | 入力欄から1行読み取る関数。読み取った値は文字列になる。 | 01-python |
| int | | 文字列を整数に変える関数。整数を表す型の名前でもある。 | 01-python |
| 整数 | | 小数点を持たない数。Python では `3` や `-7` と書く。 | 01-python |
| 変数 | variable | 値に名前を付けて残しておく仕組み。`=` の左に名前、右に値を書く。 | 01-python |
| エラー | error | Python が実行を続けられないときに出す知らせ。種類の名前と場所が出る。 | 01-python |
| f文字列 | f-string | 文字列の前に `f` を付けた書き方。`{}` の中に変数を書くと値が展開される。 | 01-python |
| str | | 値を文字列に変える関数。 | 01-python |
| 型 | type | 値の種類を表すもの。int や float などがある。 | 02-numbers |
| float | | 文字列や整数を小数に変える関数。小数を表す型の名前でもある。 | 02-numbers |
| round | | 数値を指定した桁数に丸める関数。第2引数に残す桁数を書く。 | 02-numbers |
| べき乗 | exponentiation | ある数を何回か掛け合わせる計算。Python では `**` を使う。 | 02-numbers |
| 剰余 | remainder | 割り算の余り。Python では `%` を使う。 | 02-numbers |
| 整数除算 | floor division | 割り算の商を整数で求める計算。Python では `//` を使う。 | 02-numbers |
| 優先順位 | precedence | 1つの式に複数の演算子があるとき、先に計算する順番。 | 02-numbers |
| import | | 他のモジュールの機能を使えるようにする文。 | 02-numbers |
| モジュール | module | 関連する関数や値をまとめたファイル。`import` で読み込む。 | 02-numbers |
| math | | 平方根や円周率など、数学の計算をまとめたモジュール。 | 02-numbers |
| if文 | if statement | 条件が成り立つときだけ、決まった処理を行う文。`if 条件:` の形で書く。 | 03-branch |
| 字下げ | indent | 行の頭に空白を入れること。if文などの中身であることを示す。 | 03-branch |
| else | | if文の条件が成り立たなかったときに行う処理を書く。`if` と対で使う。 | 03-branch |
| elif | | if文で3段階以上に分けるとき、前の条件が成り立たなかった場合の次の条件を書く。 | 03-branch |
| and | | 2つの条件が両方成り立つかどうかを判定する書き方。 | 03-branch |
| or | | 2つの条件のどちらかが成り立つかどうかを判定する書き方。 | 03-branch |
| not | | 条件の成り立つ・成り立たないを反転させる書き方。 | 03-branch |
| for文 | for statement | 決まった回数、処理を繰り返す文。`for 変数 in range(...):` の形で書く。 | 04-loop |
| range | | 指定した範囲の整数を順に生成する関数。for文と組み合わせて使う。 | 04-loop |
| リスト | list | 複数の値を1つの名前にまとめて残しておく仕組み。前後を `[` `]` で囲んで書く。 | 04-loop |
| 添字 | index | リストの中の値の位置を表す番号。先頭を`0`として数える。 | 04-loop |
| len | | リストに入っている値の数を返す関数。 | 04-loop |
| メソッド | method | 値の後ろに `.` を付けて呼ぶ関数。`リスト名.append(値)` のように書く。 | 04-loop |
| 要素 | element | リストや配列の中に並んでいる1つ1つの値。 | 04-loop |
| append | | リストの末尾に値を1つ加えるメソッド。 | 04-loop |
| remove | | リストから指定した値を1つ取り除くメソッド。 | 04-loop |
| while文 | while statement | 条件が成り立つ間、処理を繰り返す文。繰り返す回数を先に決めなくてよい。 | 04-loop |
| def | | 関数を定義するときに書くキーワード。`def 名前(引数):` の形で書き、次の行から字下げして中身を書く。 | 05-function |
| return | | 関数の実行を終え、呼び出した場所に値を渡す文。`return 値` の形で書く。 | 05-function |
| 既定値 | default value | 引数を渡さなかったときに使われる値。`def 名前(引数=既定値):` のように書く。 | 05-function |
| キーワード引数 | keyword argument | 呼び出すときに `名前=値` の形で渡す引数。名前で対応が決まるので、渡す順番は関係なくなる。 | 05-function |
| 有効範囲 | scope | 変数や関数の名前が使える範囲。関数の中で付けた名前は、その関数の外では使えない。 | 05-function |
| SyntaxError | | Pythonがコードの形を読み取れないときに出るエラー。コロンの不足、かっこの閉じ忘れ、全角の記号などが原因になる。 | 06-error |
| NameError | | 使った名前がどこにも定義されていないときに出るエラー。綴りの違いがよくある原因になる。 | 01-python |
| TypeError | | 値の型に合わない操作をしようとしたときに出るエラー。文字列と整数をそのまま`+`でつなぐと出る。 | 06-error |
| ValueError | | 型は合っているが、その値には変換できないときに出るエラー。`int("あ")`のように数字でない文字列を渡すと出る。 | 06-error |
| numpy | | 配列を使った数値計算をまとめて行うためのモジュール。`import numpy as np` の形で読み込む。 | 07-array |
| 配列 | array | 複数の値を1つの名前にまとめて持つ、numpyの仕組み。要素ごとの計算をまとめて行える。 | 07-array |
| 軸 | axis | 2つ以上の方向を持つ配列で、平均や合計をどの向きにまとめるかを指定するもの。`axis=0`のように書く。 | 07-array |
| mean | | 配列の値の平均を求めるメソッド。`配列名.mean()`の形で呼び出す。 | 07-array |
| 2次元配列 | 2D array | 値が縦と横の2方向に並んだ配列。行と列を持つ。`配列名.shape`で行数と列数を調べられる。 | 07-array |
| 条件での取り出し | boolean index | 配列の中から、条件を満たす値だけを取り出す書き方。`配列名[条件式]`のように書く。 | 07-array |
| スライス | slice | 配列やリストの一部を範囲で取り出す書き方。`配列名[開始:終了]`のように書き、終了の位置は含まない。 | 07-array |
| 転置 | transpose | 2次元配列の行と列を入れ替えること。`配列名.T`と書く。2行3列の配列を転置すると3行2列になる。 | 08-table |
| ブロードキャスト | broadcasting | 形の違う配列どうしを計算するとき、小さいほうを大きいほうの形に合わせて繰り返し使うしくみ。表から1行ぶんの並びを引くと、すべての行から同じ並びが引かれる。 | 08-table |
| reshape | reshape | 配列の値の並び順を変えずに、形だけを変えるメソッド。`配列名.reshape(行数, 列数)`のように書く。 | 08-table |
| ベクトル | vector | 数を1列に並べたもの。numpy では1次元の配列で表す。成分の数が2なら平面上の矢印と同じもので、3つ以上にも広げられる。 | 09-matrix |
| 内積 | inner product | 2つのベクトルの、同じ位置の成分どうしを掛けて足した1つの数。`a @ b`と書く。 | 09-matrix |
| 行列 | matrix | 数を行と列の2方向に並べたもの。numpy では2次元配列で表す。 | 09-matrix |
| 行列の積 | matrix product | 左の行列の各行と、右の行列の各列の内積を並べた行列。`A @ B`と書く。左の列の数と右の行の数が同じでないと計算できない。 | 09-matrix |
| 変化率 | rate of change | 入力が変わったとき、出力がどれだけ変わるかの割合。2点の間では `(f(b) - f(a)) / (b - a)` で求める。 | 10-slope |
| 傾き | slope | 直線が1だけ右へ進むあいだに、どれだけ上がるか。2点を結ぶ直線の傾きは、その2点の間の変化率と同じ。 | 10-slope |
| 微分 | derivative | 2点の幅を小さくしていったときに、変化率が近づいていく値。その点での接線の傾きになる。 | 10-slope |
| 偏微分 | partial derivative | 入力が2つ以上ある関数で、1つだけを動かし、ほかを止めたときの傾き。 | 10-slope |
| 確率 | probability | ある出来事の起こりやすさを、0以上1以下の数で表したもの。 | 11-probability |
| 分布 | distribution | 起こりうるそれぞれの出来事に確率を割り当てた並び。合計が1になる。 | 11-probability |
| ネイピア数 | Napier's constant | 約2.718の数。`e` と書く。numpy では `np.exp(1)` で求められる。 | 11-probability |
| 指数関数 | exponential function | `e` を何乗かする関数。`np.exp(x)` と書く。どんな数を入れても正の数になる。 | 11-probability |
| 対数 | logarithm | 指数関数の逆。`np.log(x)` は、`e` を何乗すると `x` になるかを返す。掛け算を足し算に変える。 | 11-probability |
| 分散 | variance | 値が平均からどれだけ散らばっているかを表す数。平均との差の2乗の平均。 | 11-probability |
| 標準偏差 | standard deviation | 分散の平方根。もとの値と同じ単位で散らばりを表す。 | 11-probability |
| 標準化 | standardization | 平均との差を標準偏差で割って、平均0・標準偏差1の値に直すこと。 | 11-probability |
| 偏差値 | standard score | 標準化した値を10倍して50を足したもの。平均が50、標準偏差が10になる。 | 11-probability |
| モデル | model | 入力から予測を出す、式の形。第12章では直線 `a * x + b` を使う。 | 12-predict |
| パラメータ | parameter | モデルの中にあり、データに合わせて値を決める数。第12章の直線では `a` と `b`。 | 12-predict |
| 特徴 | feature | 予測に使う入力の値。第12章では勉強時間。 | 12-predict |
| 損失 | loss | 予測と実際の値がどれだけずれているかを表す数。小さいほど当てはまりが良い。 | 12-predict |
| 平均二乗誤差 | mean squared error | 予測と実際の値の差を2乗し、平均をとった損失。人数が違うデータどうしでも比べられる。 | 12-predict |
| 勾配 | gradient | いまの位置での傾きの値。動かす変数が2つ以上のときは、それぞれの向きの傾きをまとめた配列になる。 | 13-regression |
| 勾配降下 | gradient descent | 勾配ぶん動かす（`x = x - 学習率 * 勾配`）ことをくり返し、値を小さくしていく方法。 | 13-regression |
| 学習率 | learning rate | 勾配降下で1回に動かす量を決める数。大きすぎると発散し、小さすぎると進まない。 | 13-regression |
| 発散 | divergence | 勾配降下をくり返すほど値が大きくなっていき、小さくならないこと。 | 13-regression |
| 線形回帰 | linear regression | データに直線（またはそれを広げた式）を、勾配降下などで当てはめること。 | 13-regression |
| ラベル | label | 分類で、データに付ける正解の種類を表す値。第14章では合格を1、不合格を0で表す。 | 14-classify |
| シグモイド関数 | sigmoid function | 実数を0から1の範囲に変える関数。`1 / (1 + np.exp(-z))` の形で書く。 | 14-classify |
| 交差エントロピー | cross-entropy | 正解に付けた確率の対数にマイナスを付けた損失。自信を持って外すほど大きくなる。 | 14-classify |
| ロジスティック回帰 | logistic regression | シグモイド関数で確率を予測し、交差エントロピーを勾配降下で小さくする分類の方法。 | 14-classify |
